---
phase: 02-context-plane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/claude-md-audit.sh
  - settings.json
autonomous: true
requirements:
  - CTXP-01
must_haves:
  truths:
    - "Writing a CLAUDE.md file containing an ISO 8601 timestamp is blocked with exit 2"
    - "Writing a CLAUDE.md file containing CURRENT_TASK: or ## CURRENT STATE is blocked with exit 2"
    - "Writing a CLAUDE.md file with only static reference content succeeds (exits 0)"
    - "The hook is registered in settings.json PreToolUse and installed to ~/.claude/hooks/ by install.sh"
  artifacts:
    - path: "hooks/claude-md-audit.sh"
      provides: "PreToolUse enforcement: blocks dynamic content in CLAUDE.md"
      contains: "exit 2"
    - path: "settings.json"
      provides: "Hook registration for claude-md-audit.sh"
      contains: "claude-md-audit.sh"
  key_links:
    - from: "settings.json"
      to: "hooks/claude-md-audit.sh"
      via: "PreToolUse hook registration"
      pattern: "claude-md-audit\\.sh"
    - from: "hooks/claude-md-audit.sh"
      to: "hooks/common.sh"
      via: "source common.sh — uses block() for exit-2 blocking"
      pattern: "source.*common\\.sh"
---

<objective>
Create the CLAUDE.md audit enforcement hook and register it in settings.json.

Purpose: CTXP-01 requires that no dynamic content (timestamps, PROGRESS tails, current-task notes) can be written into CLAUDE.md. A PreToolUse hook blocks any Write or Edit that would introduce dynamic patterns, keeping CLAUDE.md as a stable KV-cache-friendly reference file.

Output:
- hooks/claude-md-audit.sh — new PreToolUse hook, identical pattern to stub-reject.sh
- settings.json — updated PreToolUse array to include claude-md-audit.sh
</objective>

<execution_context>
@/Users/oanhduong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oanhduong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-context-plane/02-CONTEXT.md
@.planning/phases/02-context-plane/02-RESEARCH.md
</context>

<interfaces>
<!-- Key contracts executor needs. Extracted from codebase. -->

From hooks/common.sh:
```bash
# block(reason, fix_instruction) — emits to stderr, exits 2 (blocking)
block() {
  local reason="${1:-unspecified}"
  local fix="${2:-no fix instruction provided}"
  echo "BLOCK: ${reason}" >&2
  echo "How to fix: ${fix}" >&2
  exit 2
}
# emit(message) — non-blocking stderr message (does not exit)
emit() { echo "${1}" >&2; }
```

From hooks/stub-reject.sh (reference PreToolUse pattern):
```bash
INPUT=$(cat)
TOOL_NAME=$(echo "$INPUT" | jq -r '.tool_name // empty')
if [[ "$TOOL_NAME" != "Write" && "$TOOL_NAME" != "Edit" && "$TOOL_NAME" != "MultiEdit" ]]; then
  exit 0
fi
FILE_PATH=$(echo "$INPUT" | jq -r '.tool_input.path // empty')
FILE_CONTENT=$(echo "$INPUT" | jq -r '.tool_input.content // .tool_input.new_content // empty')
```

From settings.json (existing PreToolUse structure to extend — append, do NOT replace):
```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [{ "type": "command", "command": "bash ~/.claude/hooks/stub-reject.sh" }]
      }
    ]
  }
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create hooks/claude-md-audit.sh</name>
  <files>hooks/claude-md-audit.sh</files>
  <read_first>
    - hooks/stub-reject.sh (reference PreToolUse pattern to replicate exactly)
    - hooks/common.sh (block() and emit() signatures)
    - .planning/phases/02-context-plane/02-RESEARCH.md (Pattern 1 — full example implementation)
  </read_first>
  <behavior>
    - Test 1: echo '{"tool_name":"Write","tool_input":{"path":"CLAUDE.md","content":"Updated: 2026-06-23T10:00:00Z"}}' | bash hooks/claude-md-audit.sh; exit code MUST be 2
    - Test 2: echo '{"tool_name":"Write","tool_input":{"path":"CLAUDE.md","content":"CURRENT_TASK: my-task"}}' | bash hooks/claude-md-audit.sh; exit code MUST be 2
    - Test 3: echo '{"tool_name":"Write","tool_input":{"path":"CLAUDE.md","content":"## What This Is\nStable reference content only."}}' | bash hooks/claude-md-audit.sh; exit code MUST be 0
    - Test 4: echo '{"tool_name":"Write","tool_input":{"path":"src/app.ts","content":"Updated: 2026-06-23"}}' | bash hooks/claude-md-audit.sh; exit code MUST be 0 (non-CLAUDE.md files pass through)
    - Test 5: echo '{"tool_name":"Bash","tool_input":{"command":"ls"}}' | bash hooks/claude-md-audit.sh; exit code MUST be 0 (non-Write tools pass through)
  </behavior>
  <action>
Create hooks/claude-md-audit.sh as a new PreToolUse hook. Follow stub-reject.sh structure exactly. The hook must:

1. Shebang: #!/usr/bin/env bash
2. Comments: hook name, CTXP-01 reference, tag: architecture
3. set -euo pipefail
4. source "$(dirname "${BASH_SOURCE[0]}")/common.sh"
5. INPUT=$(cat)
6. Filter: exit 0 if TOOL_NAME is not Write, Edit, or MultiEdit
7. Read FILE_PATH from .tool_input.path // empty
8. Filter: exit 0 if FILE_PATH does not match CLAUDE.md — use: echo "$FILE_PATH" | grep -qE '(^|/)CLAUDE\.md$' || exit 0
9. Read FILE_CONTENT from .tool_input.content // .tool_input.new_content // empty; [ -z "$FILE_CONTENT" ] && exit 0
10. Pattern check 1 — ISO 8601 date: grep -qE '[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}' (anchored to datetime, not just date, to avoid false-positive on version numbers like v1.2-03). If matched: block "Dynamic content detected in CLAUDE.md: ISO 8601 timestamp found" "Move timestamps to .progress/PROGRESS.md or a session-specific file. CLAUDE.md must be static reference content only (CTXP-01 — KV-cache stability)."
11. Pattern check 2 — PROGRESS state fields: grep -qE '^(CURRENT_TASK:|VERIFY_CMD:|BLOCKED_COUNT:|## CURRENT STATE|Last updated:|Current task:)'. If matched: block "Dynamic content detected in CLAUDE.md: live-state line found (CURRENT_TASK/VERIFY_CMD/BLOCKED_COUNT/Last updated)" "Remove live-state fields from CLAUDE.md. Dynamic task state belongs in .progress/PROGRESS.md only (CTXP-01)."
12. exit 0

chmod +x hooks/claude-md-audit.sh after creating.

Key constraint from RESEARCH.md pitfall 2: use [0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2} (datetime, not just date) to avoid false-positive blocking on version numbers like 1.2-04.
  </action>
  <verify>
    <automated>
      echo '{"tool_name":"Write","tool_input":{"path":"CLAUDE.md","content":"Updated: 2026-06-23T10:00:00Z"}}' | bash hooks/claude-md-audit.sh; [ $? -eq 2 ] && echo "[PASS] timestamp blocked" || echo "[FAIL] timestamp not blocked"
      echo '{"tool_name":"Write","tool_input":{"path":"CLAUDE.md","content":"CURRENT_TASK: my-task"}}' | bash hooks/claude-md-audit.sh; [ $? -eq 2 ] && echo "[PASS] CURRENT_TASK blocked" || echo "[FAIL] CURRENT_TASK not blocked"
      echo '{"tool_name":"Write","tool_input":{"path":"CLAUDE.md","content":"## What This Is\nStable reference only."}}' | bash hooks/claude-md-audit.sh; [ $? -eq 0 ] && echo "[PASS] static content allowed" || echo "[FAIL] static content blocked"
    </automated>
  </verify>
  <done>
    hooks/claude-md-audit.sh exists, is chmod +x, sources common.sh, contains "exit 2" and "tag: architecture". Dynamic content patterns (ISO 8601 datetime and PROGRESS state fields) are blocked. Static content passes. Non-CLAUDE.md files pass through.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register claude-md-audit.sh in settings.json PreToolUse</name>
  <files>settings.json</files>
  <read_first>
    - settings.json (current structure — must read before editing to see exact JSON shape)
    - install.sh (jq merge logic — confirms PreToolUse arrays are concatenated, not replaced)
    - .planning/phases/02-context-plane/02-RESEARCH.md (settings.json PreToolUse example)
  </read_first>
  <action>
Add a second PreToolUse entry to settings.json for claude-md-audit.sh. The existing structure has one PreToolUse entry for stub-reject.sh. Add a second entry with the same shape:

```json
{
  "matcher": "Write|Edit",
  "hooks": [
    {
      "type": "command",
      "command": "bash ~/.claude/hooks/claude-md-audit.sh"
    }
  ]
}
```

Append this as the second element in the "PreToolUse" array, after the stub-reject.sh entry. Do NOT remove or modify the existing stub-reject.sh entry. All other hook arrays (PostToolUse, Stop, SessionStart) remain unchanged.

The result should be a valid JSON file where settings.json.hooks.PreToolUse has two entries: stub-reject.sh first, claude-md-audit.sh second.

Validate with: jq '.' settings.json (must exit 0 — valid JSON).
  </action>
  <acceptance_criteria>
    - grep -q 'claude-md-audit.sh' settings.json
    - grep -q 'stub-reject.sh' settings.json (existing entry preserved)
    - jq '.hooks.PreToolUse | length' settings.json returns 2
    - jq '.' settings.json exits 0 (valid JSON)
  </acceptance_criteria>
  <verify>
    <automated>
      grep -q 'claude-md-audit.sh' settings.json && echo "[PASS] hook registered" || echo "[FAIL] hook not registered"
      jq '.hooks.PreToolUse | length' settings.json | grep -q '^2$' && echo "[PASS] 2 PreToolUse entries" || echo "[FAIL] wrong PreToolUse count"
      jq '.' settings.json > /dev/null && echo "[PASS] valid JSON" || echo "[FAIL] invalid JSON"
    </automated>
  </verify>
  <done>
    settings.json contains claude-md-audit.sh in PreToolUse array. PreToolUse has exactly 2 entries. stub-reject.sh entry is preserved. JSON is valid.
  </done>
</task>

</tasks>

<verification>
Phase 2 Plan 01 checks:
1. hooks/claude-md-audit.sh exists: test -f hooks/claude-md-audit.sh
2. Hook is executable: test -x hooks/claude-md-audit.sh
3. Hook sources common.sh: grep -q 'source.*common.sh' hooks/claude-md-audit.sh
4. Hook has architecture tag: grep -q 'tag: architecture' hooks/claude-md-audit.sh
5. Dynamic content blocked: echo '{"tool_name":"Write","tool_input":{"path":"CLAUDE.md","content":"Updated: 2026-06-23T10:00:00Z"}}' | bash hooks/claude-md-audit.sh; [ $? -eq 2 ]
6. Static content passes: echo '{"tool_name":"Write","tool_input":{"path":"CLAUDE.md","content":"## Static"}}' | bash hooks/claude-md-audit.sh; [ $? -eq 0 ]
7. settings.json registers hook: grep -q 'claude-md-audit.sh' settings.json
8. Valid JSON: jq '.' settings.json > /dev/null
</verification>

<success_criteria>
- hooks/claude-md-audit.sh exists, is chmod +x, tagged architecture, sources common.sh
- Writing a CLAUDE.md with ISO 8601 datetime (e.g., 2026-06-23T10:00:00Z) exits 2
- Writing a CLAUDE.md with CURRENT_TASK: exits 2
- Writing a CLAUDE.md with static reference content exits 0
- Writing a non-CLAUDE.md file with dynamic content exits 0 (scope is CLAUDE.md only)
- settings.json has claude-md-audit.sh in PreToolUse array; existing stub-reject.sh entry preserved
- jq '.' settings.json exits 0
</success_criteria>

<output>
After completion, create .planning/phases/02-context-plane/02-01-SUMMARY.md
</output>
