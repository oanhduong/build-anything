---
phase: 03-self-improve-loop
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - scripts/retro-e2e-test.sh
autonomous: false
requirements: [SELF-01, SELF-02, SELF-03, SELF-04, SELF-05, SELF-06, SELF-07, SELF-08, SELF-09]

must_haves:
  truths:
    - "scripts/retro-e2e-test.sh exits 0 and prints '[PASS]' for every Phase 3 check, '0 failed' in the summary"
    - "The e2e test injects a synthetic hit count and synthetic trace, runs auto-distill, and asserts a candidate appears in pending/ with an evidence: field"
    - "The e2e test verifies duplicate suppression (second run adds no new candidate)"
    - "The e2e test approves the candidate and asserts it lands in failure-lib/ (committed)"
    - "Stop-hook regression guards (force-loop-test.sh, no-verify-cmd-test.sh) pass as part of the gate"
  artifacts:
    - path: "scripts/retro-e2e-test.sh"
      provides: "Phase 3 done command — 8-step e2e sequence covering SELF-01..09"
      contains: "[PASS]"
  key_links:
    - from: "scripts/retro-e2e-test.sh"
      to: "scripts/auto-distill.sh"
      via: "direct call with synthetic temp trace"
      pattern: "auto-distill.sh"
    - from: "scripts/retro-e2e-test.sh"
      to: "failure-lib/pending/ and failure-lib/"
      via: "assert candidate drafted then approved"
      pattern: "pending"
---

<objective>
Build `scripts/retro-e2e-test.sh` — the Phase 3 done command. It runs the full self-improve loop end-to-end against isolated `mktemp -d` fixtures (never touching real `.progress/` or `~/.claude/failure-lib`), asserting all 8 steps from CONTEXT.md, then gate-checks the Stop-hook regression suite. Finally a human checkpoint confirms the loop and the prune empty-set behavior.

Purpose: This is the binary, externally-checkable proof that Phase 3's compounding loop works. It is the ROADMAP §Phase 3 done command.
Output: `scripts/retro-e2e-test.sh` + GIAV-style human sign-off recorded in PROGRESS.
</objective>

<execution_context>
@/Users/oanhduong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oanhduong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-self-improve-loop/03-CONTEXT.md
@.planning/phases/03-self-improve-loop/03-RESEARCH.md
@.planning/phases/03-self-improve-loop/03-01-SUMMARY.md
@.planning/phases/03-self-improve-loop/03-02-SUMMARY.md
@.planning/phases/03-self-improve-loop/03-03-SUMMARY.md

<interfaces>
<!-- Everything this test exercises, from Plans 01-03. -->
scripts/auto-distill.sh <trace-file> [progress-file] [failure-lib-dir]
  - exit 2 + "trace required" if no trace (SELF-01)
  - drafts model-crutch candidate(s) into <failure-lib-dir>/pending/ with evidence: line (SELF-02)
  - dedups against <failure-lib-dir>/*.md by id (SELF-05)

Trace line format (synthetic fixture must mimic): `TIMESTAMP TOOL TARGET EXIT_CODE`
  e.g. `2026-06-23T14:02:11Z Bash npm-test 1`
lesson-hit-counts.json schema: flat `{ "<id>": <count> }`

Test harness style (mirror scripts/replay-giavico-failures.sh):
  pass()/fail() helpers, `[PASS] <id>: <desc>` / `[FAIL] ...`, final `Results: N passed, M failed`, exit non-zero iff any fail.
Isolation style (mirror scripts/context-reset-test.sh): WORK=$(mktemp -d).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build scripts/retro-e2e-test.sh (Phase 3 done command)</name>
  <read_first>
    - scripts/replay-giavico-failures.sh (full — pass/fail helpers, [PASS]/[FAIL] format, final summary, exit code; optional install pre-step pattern)
    - scripts/context-reset-test.sh (full — mktemp -d isolation idiom, synthetic-fixture writing into $TMP)
    - .planning/phases/03-self-improve-loop/03-CONTEXT.md "Done command and e2e test" (the locked 8-step sequence)
    - .planning/phases/03-self-improve-loop/03-RESEARCH.md "Test harness skeleton" + Open Q3 (mktemp vs install pre-step → lean mktemp)
    - scripts/auto-distill.sh (the unit under test — confirm its arg contract from the source after Plan 01)
    - skills/retro/SKILL.md (the approve behavior the test simulates: move pending/<id>.md → failure-lib/<id>.md + git commit)
  </read_first>
  <files>scripts/retro-e2e-test.sh</files>
  <action>
    Create `scripts/retro-e2e-test.sh`, executable. Header comment ending `# tag: architecture`. Shebang `#!/usr/bin/env bash`, `set -uo pipefail` (NOT set -e). Define `HARNESS_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"`, `PASS=0; FAIL=0`, and `pass()/fail()` helpers exactly mirroring replay-giavico-failures.sh (`[PASS] $1` / `[FAIL] $1` to stderr, increment counters).

    Use `mktemp -d` isolation (Open Q3 recommendation): `WORK=$(mktemp -d)`. Build an isolated failure-lib fixture there: `LIB="$WORK/failure-lib"; mkdir -p "$LIB/pending"`. Copy 1-2 real lessons in so dedup has something to grep against (e.g. `cp "$HARNESS_DIR/failure-lib/openpyxl-engine.md" "$LIB/"`). Build `PROG="$WORK/.progress"; mkdir -p "$PROG"`.

    Implement the locked 8-step sequence, each as a `pass`/`fail` assertion:

    1. SELF-01 guard: run `bash "$HARNESS_DIR/scripts/auto-distill.sh"` with NO arg; assert exit code == 2 AND stderr contains `trace required`. → `[PASS] SELF-01: distill blocked without trace`
    2. Inject synthetic hit count: `echo '{"openpyxl-engine": 3}' > "$PROG/lesson-hit-counts.json"` (proves the >=3 threshold value is well-formed for the repeated-failure trigger). Assert `jq '."openpyxl-engine"' == 3`.
    3. Inject a synthetic trace with one NEW non-zero error line: `printf '%s Bash npm-test 1\n' "$(date -u +%Y-%m-%dT%H:%M:%SZ)" > "$WORK/trace.log"`.
    4. Run distill against the fixture: `bash "$HARNESS_DIR/scripts/auto-distill.sh" "$WORK/trace.log" "$PROG/PROGRESS.md" "$LIB"`. Assert a candidate `.md` (other than .gitkeep) now exists in `$LIB/pending/`. → `[PASS] SELF-03: candidate drafted to pending on distill`
    5. SELF-02: assert the drafted candidate contains an `^evidence:` line: `grep -q "^evidence:" "$LIB"/pending/*.md`. → `[PASS] SELF-02: candidate carries evidence`
    6. SELF-05 duplicate suppression: record the candidate count, run distill AGAIN with the same trace, assert the pending count did NOT increase. → `[PASS] SELF-05: duplicate suppressed on re-run`
    7. SELF-06 approve simulation (the test reproduces what /retro approve does, against the fixture — does NOT mutate real ~/.claude): pick the candidate id, move `$LIB/pending/<id>.md` → `$LIB/<id>.md`. Initialize a throwaway git repo in `$WORK` (`git -C "$WORK" init -q; git -C "$WORK" add -A; git -C "$WORK" commit -qm "test approve" || true`) and assert the lesson file is now under `$LIB/` and tracked. → `[PASS] SELF-06: approved lesson lands in failure-lib + committed`
    8. SELF-09 scope guard: assert that across the whole run, the distiller never wrote to a top-level `$LIB/*.md` directly (only via the simulated approve in step 7) and the candidate it drafted was `model-crutch`-tagged, never `architecture`: `grep -q 'model-crutch' "$LIB/<id>.md" && ! grep -q 'tag: architecture' "$LIB"/pending/*.md 2>/dev/null` (no pending files left is also fine). → `[PASS] SELF-09: candidate model-crutch, never architecture`

    Then run the REGRESSION guard (Stop hook untouched — required by VALIDATION.md "Before /gsd:verify-work"):
    9. `bash "$HARNESS_DIR/scripts/force-loop-test.sh" >/dev/null 2>&1` → pass/fail `LOOP regression`
    10. `bash "$HARNESS_DIR/scripts/no-verify-cmd-test.sh" >/dev/null 2>&1` → pass/fail `PLAN-01 regression`

    Clean up: `rm -rf "$WORK"`. Final: `echo "Results: $PASS passed, $FAIL failed"`; `[ "$FAIL" -eq 0 ]` as the last statement (sets exit code). Do NOT call install.sh (mktemp isolation makes it unnecessary — Open Q3 lean-mktemp; avoids mutating real ~/.claude).

    SELF-04 / SELF-08 note: SELF-04 (load-lessons pending notice) and SELF-08 (skill calls auto-distill) are verified structurally — add two lightweight assertions: `grep -q '/retro approve' "$HARNESS_DIR/hooks/load-lessons.sh"` → `[PASS] SELF-04` and `grep -q 'auto-distill.sh' "$HARNESS_DIR/skills/retro/SKILL.md"` → `[PASS] SELF-08`.
  </action>
  <verify>
    <automated>chmod +x scripts/retro-e2e-test.sh; bash scripts/retro-e2e-test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/retro-e2e-test.sh` exists and is executable
    - Running it exits 0 and prints `0 failed` in the summary
    - Output contains `[PASS]` lines for SELF-01, SELF-02, SELF-03, SELF-05, SELF-06, SELF-09 (one each)
    - Output contains regression `[PASS]` for LOOP and PLAN-01 guards
    - Output contains structural `[PASS]` for SELF-04 and SELF-08
    - The test uses `mktemp -d` and never writes to the real `.progress/` or `$HOME/.claude/failure-lib`: `grep -q 'mktemp -d' scripts/retro-e2e-test.sh`
    - `grep -q 'rm -rf' scripts/retro-e2e-test.sh` (cleans up the temp dir)
  </acceptance_criteria>
  <done>retro-e2e-test.sh runs the full 8-step loop in isolation, asserts SELF-01..09 coverage + Stop-hook regression, exits 0 with 0 failed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — self-improve loop + prune empty-set</name>
  <files>.progress/PROGRESS.md</files>
  <action>
    This is a human-verification checkpoint. After Task 1 is green, present the verification steps below to the human and pause for sign-off. Do not proceed past this gate until the human types "approved".
  </action>
  <what-built>
    The complete Phase 3 self-improve loop: auto-distill.sh (engine), the three hook extensions (hit tracking, threshold triggers, pending notice), the /retro skill (approve/run/prune), and the retro-e2e-test.sh done command.
  </what-built>
  <how-to-verify>
    1. Run the done command: `bash scripts/retro-e2e-test.sh` — confirm it prints `Results: N passed, 0 failed` and exits 0.
    2. Confirm SELF-01 manually: `bash scripts/auto-distill.sh; echo "exit=$?"` — expect a "trace required" message and `exit=2`.
    3. Confirm the regression guards independently: `bash scripts/force-loop-test.sh && bash scripts/no-verify-cmd-test.sh && echo "regression OK"`.
    4. SELF-07 prune empty-set (manual — per VALIDATION.md Manual-Only): invoke `/retro prune` in a Claude Code session against the current failure-lib (which has 0 model-crutch entries) and confirm it responds gracefully with "No model-crutch rules to prune." and does not error.
    5. Record GIAV-style sign-off in `.progress/PROGRESS.md`: add a line `SELF-PHASE3: PASS` to the HISTORY LOG.
  </how-to-verify>
  <verify>
    <automated>bash scripts/retro-e2e-test.sh</automated>
  </verify>
  <acceptance_criteria>
    - retro-e2e-test.sh exits 0 with "0 failed"
    - /retro prune tolerates the empty model-crutch set gracefully (SELF-07 manual confirmation)
    - SELF-PHASE3: PASS recorded in PROGRESS HISTORY LOG
  </acceptance_criteria>
  <done>Done command green, prune empty-set graceful, SELF-PHASE3: PASS recorded in PROGRESS.</done>
  <resume-signal>Type "approved" once the done command exits 0 and /retro prune behaves gracefully, or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- `bash scripts/retro-e2e-test.sh` exits 0, "0 failed"
- `bash scripts/auto-distill.sh; echo $?` → 2 ("trace required")
- `bash scripts/force-loop-test.sh && bash scripts/no-verify-cmd-test.sh` exit 0 (Stop hook untouched)
- /retro prune handles empty model-crutch set gracefully (human-verified)
</verification>

<success_criteria>
- ROADMAP §Phase 3 done command (`./scripts/retro-e2e-test.sh`) exits 0
- All 5 ROADMAP success criteria for Phase 3 demonstrated by the test
- SELF-01..09 each covered by an assertion or structural check
- Human sign-off recorded (SELF-PHASE3: PASS)
</success_criteria>

<output>
After completion, create `.planning/phases/03-self-improve-loop/03-04-SUMMARY.md`
</output>
