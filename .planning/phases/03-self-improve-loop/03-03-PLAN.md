---
phase: 03-self-improve-loop
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - skills/retro/SKILL.md
  - install.sh
autonomous: true
requirements: [SELF-06, SELF-07, SELF-08, SELF-09]

must_haves:
  truths:
    - "/retro run <trace-file> calls scripts/auto-distill.sh with the trace; /retro run with no arg errors with 'trace required'"
    - "/retro approve batch-reviews failure-lib/pending/*.md, moves approved candidates to failure-lib/, deletes rejected, and commits approved to ~/.claude via git"
    - "/retro prune reviews model-crutch-tagged failure-lib rules and tolerates the empty set gracefully (no error, exit 0)"
    - "The skill holds NO distillation logic of its own — /retro run shells out to auto-distill.sh (SELF-08 single source of truth)"
    - "install.sh copies the retro skill into ~/.claude/skills/retro/"
  artifacts:
    - path: "skills/retro/SKILL.md"
      provides: "/retro approve|run|prune orchestration skill (no distill logic — calls auto-distill.sh)"
      contains: "auto-distill.sh"
    - path: "install.sh"
      provides: "Skill copy already globs skills/*/ — confirm retro is picked up; pending/ stays out of failure-lib install"
  key_links:
    - from: "skills/retro/SKILL.md"
      to: "scripts/auto-distill.sh"
      via: "/retro run forwards trace arg"
      pattern: "auto-distill.sh"
    - from: "skills/retro/SKILL.md"
      to: "~/.claude (git)"
      via: "approve commits moved lesson"
      pattern: "git -C"
---

<objective>
Create the `/retro` skill — a thin orchestration layer over `auto-distill.sh` with three subcommands (`approve`, `run`, `prune`) — and confirm `install.sh` installs it. The skill holds NO distillation logic (SELF-08); it shells out to the Plan 01 script and performs interactive review + git commit of approved lessons (SELF-06).

Purpose: This is the human approval gate (SELF-09) and the manual override path (SELF-08). It also implements the prune step (SELF-07) and the approve→commit step (SELF-06).
Output: `skills/retro/SKILL.md`, edits/confirmation to `install.sh`.
</objective>

<execution_context>
@/Users/oanhduong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oanhduong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-self-improve-loop/03-CONTEXT.md
@.planning/phases/03-self-improve-loop/03-RESEARCH.md
@.planning/phases/03-self-improve-loop/03-01-SUMMARY.md

<interfaces>
<!-- auto-distill.sh contract from Plan 01. -->
scripts/auto-distill.sh <trace-file> [progress-file] [failure-lib-dir]
  - exits 2 + "trace required" if trace arg missing (SELF-01 — /retro run inherits this)
  - drafts candidates to failure-lib/pending/

Existing skill conventions (from skills/handoff/SKILL.md — follow this style):
- YAML frontmatter: name, description, optionally `disable-model-invocation: true` for direct-action skills
- Body: numbered procedural steps in markdown, instructions to Claude (not executable code)

Live failure-lib frontmatter (target after approval — SELF-06, no enforcement-type):
  id / tags / when / error-match  (+ optional evidence: retained for provenance)

git pattern (from install.sh lines 76-84): `git -C "${HOME}/.claude" add ... && git -C "${HOME}/.claude" commit -m "..."`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write skills/retro/SKILL.md with approve | run | prune subcommands</name>
  <read_first>
    - skills/handoff/SKILL.md (skill frontmatter + numbered-step style to mirror)
    - skills/context-pull/SKILL.md (a multi-operation skill for structure reference)
    - .planning/phases/03-self-improve-loop/03-CONTEXT.md "/retro skill structure" section (the 3 subcommands' exact behavior — locked)
    - .planning/phases/03-self-improve-loop/03-RESEARCH.md Pattern 4 + Pitfall 6 (prune empty-set tolerance) + SELF-07 row
    - .planning/phases/03-self-improve-loop/03-01-SUMMARY.md (auto-distill.sh path + arg contract)
  </read_first>
  <files>skills/retro/SKILL.md</files>
  <action>
    Create `skills/retro/SKILL.md`. Frontmatter:
    ```
    ---
    name: retro
    description: Self-improve loop control. Subcommands — `approve` (batch-review pending candidate lessons, commit approved to failure-lib), `run <trace-file>` (manual distill override), `prune` (retire stale model-crutch rules). Use when reviewing the pending-lessons queue or manually distilling from a trace.
    ---
    ```
    (Do NOT set `disable-model-invocation: true` — approve/prune are interactive review loops requiring model reasoning, unlike the handoff direct-action skill.)

    Body — three clearly headed subcommand sections. The skill is INSTRUCTIONS to Claude; it must hold NO distill logic (SELF-08 — all distillation is in scripts/auto-distill.sh). Resolve `LIB_DIR="$HOME/.claude/failure-lib"` and `PENDING_DIR="$LIB_DIR/pending"` in the steps.

    ## `/retro run <trace-file>` (SELF-08, SELF-01)
    1. If no `<trace-file>` argument was given, STOP and tell the user: "trace required — usage: /retro run <trace-file>". (Do not call the script with an empty arg.)
    2. Call: `bash ~/.claude/scripts/auto-distill.sh "<trace-file>"` (or the source `scripts/auto-distill.sh` if running from the repo). This is the SAME script the Stop hook uses — single source of truth. Report the candidate count it emits.
    3. Tell the user to run `/retro approve` to review the drafted candidates.

    ## `/retro approve` (SELF-06, SELF-09)
    1. List all `$PENDING_DIR/*.md` (excluding `.gitkeep`). If none, say "No pending candidates." and stop.
    2. For EACH candidate, display: id, tags, the `evidence:` line, and the "What happened"/"How to avoid" body. Then prompt the human `y` (approve) / `n` (reject) — or accept `all` to approve every remaining candidate in one batch.
    3. For an APPROVED candidate `<id>.md`: move it from `$PENDING_DIR/<id>.md` to `$LIB_DIR/<id>.md` (the file is already in the live failure-lib format — id/tags/when/error-match; the evidence: line MAY be retained for provenance — SELF-06 needs NO format conversion). Then commit to the versioned ~/.claude repo: `git -C "$HOME/.claude" add "failure-lib/<id>.md" && git -C "$HOME/.claude" commit -m "retro: approve lesson <id> $(date -u +%Y-%m-%dT%H:%M:%SZ)"`.
    4. For a REJECTED candidate: delete `$PENDING_DIR/<id>.md` (discard — SELF-06 "rejected lesson is discarded").
    5. Summarize: N approved (committed), M rejected.
    Note for the human: approved lessons are surfaced automatically by existing hooks (load-lessons.sh, lessons-post-write.sh, lessons-on-error.sh) — no separate conversion step (SELF-06).

    ## `/retro prune` (SELF-07)
    1. Find all `$LIB_DIR/*.md` whose `tags:` line contains `model-crutch`. If NONE, say "No model-crutch rules to prune." and exit 0 (Pitfall 6 — the live library currently has zero model-crutch entries; MUST tolerate the empty set gracefully).
    2. For each model-crutch rule, parse the model-version token from the tag (e.g. `claude-sonnet-4-6`). Compare against the current model version (ask the human what the current model version is, or accept it as an argument). Show which rules carry a version OLDER than current.
    3. Prompt the human per stale rule: retire (`y`) or keep (`n`).
    4. For each retired rule: delete `$LIB_DIR/<id>.md` and commit: `git -C "$HOME/.claude" add -A && git -C "$HOME/.claude" commit -m "retro: prune stale model-crutch rule <id> $(date -u +%Y-%m-%dT%H:%M:%SZ)"`.
    5. Summarize retired vs kept.

    End the file with a one-line note: "All distillation logic lives in scripts/auto-distill.sh — this skill only orchestrates review, commit, and prune (SELF-08)."
  </action>
  <verify>
    <automated>test -f skills/retro/SKILL.md && grep -q 'auto-distill.sh' skills/retro/SKILL.md && grep -q 'git -C' skills/retro/SKILL.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `skills/retro/SKILL.md` exists with `name: retro` in frontmatter
    - Contains `auto-distill.sh` (SELF-08 — run shells out to the script)
    - Contains literal `trace required` (SELF-01 propagated to /retro run)
    - Contains `git -C "$HOME/.claude"` (SELF-06 commit of approved lesson)
    - Contains `No model-crutch rules to prune` (SELF-07 empty-set tolerance)
    - Contains a per-candidate review prompt and an `all` batch option (approve UX)
    - Does NOT embed distillation/pattern-detection logic (no trace-parsing loops) — grep for absence of in-skill distill code: skill must reference `auto-distill.sh` rather than re-implement
  </acceptance_criteria>
  <done>/retro skill orchestrates run (→auto-distill.sh), approve (move+commit / discard), prune (stale model-crutch, empty-set safe); holds no distill logic.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm install.sh installs the retro skill and keeps pending/ out of the failure-lib install</name>
  <read_first>
    - install.sh lines 31-48 (failure-lib seed loop uses failure-lib/*.md flat; skills copy loops over skills/*/)
    - .planning/phases/03-self-improve-loop/03-RESEARCH.md Pitfall 1 (source vs installed path; failure-lib never-overwrite)
  </read_first>
  <files>install.sh</files>
  <action>
    Verify and (only if needed) adjust install.sh:
    1. The skills copy loop (lines 41-47) globs `skills/*/` so `skills/retro/` is picked up automatically — confirm by reading it. No change needed unless the glob excludes new dirs. If it already loops `for skill_dir in "${HARNESS_DIR}/skills/"*/`, the retro skill installs with zero edits — add a comment noting retro is covered, do NOT rewrite the loop.
    2. The failure-lib seed loop (lines 32-38) uses `"${HARNESS_DIR}/failure-lib/"*.md` — a FLAT glob that does NOT recurse into `pending/`. This is correct: the pending queue is runtime-only and must never be installed to ~/.claude. Confirm the glob is flat (`*.md`, not `**` or `-r`). If somehow it copies subdirs, constrain it to `*.md` flat. Add a comment: `# pending/ is runtime-only — flat *.md glob intentionally excludes it`.
    3. Make NO other changes. If both behaviors are already correct (expected from inspection), the only edit is adding the two clarifying comments so the intent is encoded for future maintainers.
  </action>
  <verify>
    <automated>bash -n install.sh && grep -q 'skills/' install.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n install.sh` passes
    - install.sh skills loop still globs `skills/`*/ (retro picked up): `grep -q 'skills/"\*/' install.sh` OR equivalent skills glob present
    - failure-lib seed glob is flat `*.md` (does not recurse into pending/): `grep -q 'failure-lib/"\*.md' install.sh`
    - A comment notes pending/ is runtime-only / retro is covered by the skills loop
  </acceptance_criteria>
  <done>install.sh installs skills/retro/ via the existing skills glob; failure-lib flat glob keeps pending/ out of the install; intent documented in comments.</done>
</task>

</tasks>

<verification>
- `skills/retro/SKILL.md` exists, references auto-distill.sh, commits approved lessons via git, tolerates empty model-crutch set
- `install.sh` skills loop covers retro; failure-lib glob stays flat (pending excluded)
</verification>

<success_criteria>
- SELF-06: approve moves candidate to failure-lib and commits to ~/.claude; rejected discarded; no format conversion
- SELF-07: prune reviews model-crutch rules, empty-set safe
- SELF-08: /retro run uses the same auto-distill.sh — no duplicated logic
- SELF-09: human approval gate; candidates only enter failure-lib via approve
</success_criteria>

<output>
After completion, create `.planning/phases/03-self-improve-loop/03-03-SUMMARY.md`
</output>
