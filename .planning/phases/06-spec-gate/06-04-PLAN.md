---
phase: 06-spec-gate
plan: 04
type: execute
wave: 3
depends_on: ["02", "03"]
files_modified: []
autonomous: false
requirements: [GATE-01, GATE-04]
must_haves:
  truths:
    - "Running /spec on a real toy task asks the 3 questions, proposes a draft, and writes nothing until the human types 'confirm'"
    - "After 'confirm', .progress/SPEC.md exists with a confirm-token: field and .progress/PROGRESS.md VERIFY_CMD is non-empty and derived from the first criterion"
    - "A code Write after confirm passes the gate; tampering the criteria re-blocks it"
  artifacts: []
  key_links:
    - from: "/spec skill"
      to: ".progress/SPEC.md + .progress/PROGRESS.md"
      via: "interactive confirm then token write then VERIFY_CMD derivation"
      pattern: "confirm-token"
---

<objective>
Human-verify the interactive GATE-01 happy path end to end. The /spec skill is interactive (requires a human to type "confirm"), so this behavior cannot be fully automated — plan 03 automated everything derivable (token round-trip via Binary F); this plan confirms the live skill invocation and VERIFY_CMD derivation on a real toy task.

Purpose: Confirm GATE-01 (interview to confirm to written spec) and GATE-04 (VERIFY_CMD derived at confirm time) in a live session, closing Phase 6.
Output: Human sign-off; no code changes.
</objective>

<execution_context>
@/Users/oanhduong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oanhduong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-spec-gate/06-CONTEXT.md
@.planning/phases/06-spec-gate/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy the harness and run the full automated suite</name>
  <files>~/.claude/hooks/stub-reject.sh, ~/.claude/skills/spec/SKILL.md (deploy targets via install.sh; no source files modified)</files>
  <read_first>
    - install.sh (skills glob at lines 50-55 deploys skills/spec/ to ~/.claude/skills/spec)
    - scripts/test-spec-gate.sh (final GREEN suite)
  </read_first>
  <action>
Run `bash install.sh` to deploy the modified `hooks/stub-reject.sh` and the new `skills/spec/SKILL.md` to `~/.claude`. install.sh globs `skills/*/`, so no install.sh edit is needed — confirm the glob picks up skills/spec/. Then run the full suite to confirm no regression before the interactive check:
```
bash scripts/test-enforcement.sh && bash scripts/test-verifier-independence.sh && bash scripts/test-spec-gate.sh && bash preflight.sh
```
Confirm `~/.claude/skills/spec/SKILL.md` exists after install and `~/.claude/hooks/stub-reject.sh` contains the string `SPEC.md absent`.
  </action>
  <verify>
    <automated>bash /Users/oanhduong/Work/mine/build-anything/scripts/test-spec-gate.sh && test -f "$HOME/.claude/skills/spec/SKILL.md" && grep -qF "SPEC.md absent" "$HOME/.claude/hooks/stub-reject.sh"</automated>
  </verify>
  <acceptance_criteria>
    - `bash install.sh` exits 0
    - `test -f "$HOME/.claude/skills/spec/SKILL.md"` (skill deployed)
    - `grep -qF 'SPEC.md absent' "$HOME/.claude/hooks/stub-reject.sh"` (gated hook deployed)
    - `bash scripts/test-enforcement.sh` exits 0
    - `bash scripts/test-verifier-independence.sh` exits 0
    - `bash scripts/test-spec-gate.sh` exits 0
    - `bash preflight.sh` exits 0
  </acceptance_criteria>
  <done>Modified stub-reject.sh and new /spec skill are installed to ~/.claude; all automated suites and preflight are green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live /spec happy-path verification</name>
  <files>none — interactive verification, no source files modified</files>
  <action>Pause for the human to run the live /spec happy path per the how-to-verify steps. This flow is interactive (the skill requires a typed literal "confirm"), so it cannot be automated. Present the steps, then wait for the resume signal before marking the phase complete.</action>
  <what-built>
    The /spec skill (interview to confirm to SPEC.md write to VERIFY_CMD derivation) and the stub-reject spec gate (absent/unconfirmed/tampered blocks + .progress/SPEC.md exemption), deployed to ~/.claude in Task 1.
  </what-built>
  <how-to-verify>
    In a scratch directory with a `.progress/PROGRESS.md` present (or run bootstrap-project.sh first):

    1. Invoke `/spec add a hello function to greet.sh`.
    2. Confirm the skill asks the THREE questions: "What can go wrong", "What does 'done' look like", "smallest safe change". Answer them.
    3. Confirm the skill PROPOSES a draft SPEC.md inline and writes NOTHING yet. Try replying "looks good" — confirm it does NOT write the file (only the literal word "confirm" should).
    4. Type the literal word `confirm`.
    5. Confirm `.progress/SPEC.md` now exists: run `grep '^confirm-token:' .progress/SPEC.md` — a non-empty sha256 hex string must be present, and `confirm-token: PENDING` must NOT remain.
    6. Confirm VERIFY_CMD was derived: run `grep '^VERIFY_CMD:' .progress/PROGRESS.md` — must be non-empty and reflect the first criterion.
    7. Confirm the gate now opens: attempt a normal code Write (e.g. to `greet.sh`) — it should pass (no "SPEC.md" block).
    8. Confirm tamper re-blocks: manually edit a criterion line in `.progress/SPEC.md`, then attempt another code Write — it must be blocked with "SPEC.md token invalid".
  </how-to-verify>
  <verify>Manual — human confirms all 8 steps behave as described.</verify>
  <done>Human types "approved" after all 8 steps pass; Phase 6 spec gate is verified live.</done>
  <resume-signal>Type "approved" if all 8 steps behave as described, or describe what deviated.</resume-signal>
</task>

</tasks>

<verification>
- Task 1: full automated suite + preflight green; skill and gated hook deployed to ~/.claude.
- Task 2 (human): 3-question interview, no write before literal "confirm", SPEC.md written with real confirm-token, VERIFY_CMD derived, gate opens on valid spec and re-blocks on tampered criteria.
</verification>

<success_criteria>
Phase 6 is complete: the spec gate is live and enforced, and the interactive /spec happy path is human-verified end to end (GATE-01, GATE-04).
</success_criteria>

<output>
After completion, create `.planning/phases/06-spec-gate/06-04-SUMMARY.md`
</output>
