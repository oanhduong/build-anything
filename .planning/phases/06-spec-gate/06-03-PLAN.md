---
phase: 06-spec-gate
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - skills/spec/SKILL.md
  - scripts/test-spec-gate.sh
autonomous: true
requirements: [GATE-01, GATE-04]
must_haves:
  truths:
    - "/spec asks exactly three fixed risk questions before proposing anything"
    - "/spec proposes a draft SPEC.md and does NOT write any file until the human types the literal word 'confirm'"
    - "On confirm, /spec computes shasum -a 256 of the ## Acceptance Criteria section and writes .progress/SPEC.md with that confirm-token"
    - "The token /spec writes is accepted by stub-reject (identical awk+sed+shasum pipeline) — a subsequent code Write passes the gate"
    - "On confirm, /spec derives VERIFY_CMD from the first criterion and updates .progress/PROGRESS.md VERIFY_CMD via sed"
  artifacts:
    - path: "skills/spec/SKILL.md"
      provides: "Full /spec skill: 3-question interview, confirm gate, token computation, SPEC.md write, PROGRESS.md VERIFY_CMD derivation"
      contains: "shasum -a 256"
      min_lines: 45
  key_links:
    - from: "skills/spec/SKILL.md"
      to: ".progress/SPEC.md"
      via: "Write tool (exempt path) with confirm-token computed from written file via canonical pipeline"
      pattern: "confirm-token"
    - from: "skills/spec/SKILL.md"
      to: ".progress/PROGRESS.md"
      via: "sed -i.bak in-place VERIFY_CMD update using | delimiter"
      pattern: "VERIFY_CMD"
---

<objective>
Implement the full `/spec` skill in `skills/spec/SKILL.md`, replacing the wave-0 scaffold. The skill runs a fixed 3-question risk interview, proposes a draft SPEC.md, blocks on the literal word "confirm", then computes the confirm-token and writes `.progress/SPEC.md` plus updates PROGRESS.md `VERIFY_CMD`.

The token computation MUST be byte-identical to stub-reject's (plan 02). To guarantee this by construction, the skill writes SPEC.md with `confirm-token: PENDING`, computes the token from the WRITTEN FILE using the exact same awk+sed+shasum pipeline stub-reject uses, then patches the token line in — so both sides hash identical bytes and Pitfall 2 (normalization drift) cannot occur.

Purpose: Close GATE-01 (risk-driven interview, human-confirm before write) and GATE-04 (VERIFY_CMD derived from criteria, not free-form).
Output: Full `skills/spec/SKILL.md`; `scripts/test-spec-gate.sh` gains a Binary F round-trip case.
</objective>

<execution_context>
@/Users/oanhduong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oanhduong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-spec-gate/06-CONTEXT.md
@.planning/phases/06-spec-gate/06-RESEARCH.md

<interfaces>
SPEC.md format the skill must produce (from CONTEXT.md, locked):
```
---
task: <task name>
confirm-token: <sha256>
confirmed-at: <ISO 8601 UTC>
---

## Risk List
- <risk from Q1>

## Acceptance Criteria
1. <criterion>
2. <criterion>

## Verify Command
<prose note describing what VERIFY_CMD checks>
```

CANONICAL token pipeline — MUST be byte-identical to stub-reject.sh (plan 02):
```
awk '/^## Acceptance Criteria$/{in_sec=1;next} in_sec && /^## /{exit} in_sec{print}' "$SPEC_FILE" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//' | shasum -a 256 | cut -d' ' -f1
```

In-place PROGRESS.md VERIFY_CMD update (| delimiter avoids path-slash conflicts — Pitfall 6):
```
sed -i.bak "s|^VERIFY_CMD:.*|VERIFY_CMD: ${DERIVED_CMD}|" "$PROGRESS_FILE" && rm -f "${PROGRESS_FILE}.bak"
```

Existing skill prose format: skills/retro/SKILL.md (frontmatter name/description, numbered step prose, fenced bash blocks, $ARGUMENTS usage).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the full /spec skill (interview + confirm gate + token + SPEC.md write + VERIFY_CMD)</name>
  <files>skills/spec/SKILL.md</files>
  <read_first>
    - skills/spec/SKILL.md (the wave-0 scaffold being replaced)
    - skills/retro/SKILL.md (skill prose structure: numbered steps, fenced bash, $ARGUMENTS usage)
    - hooks/stub-reject.sh (AFTER plan 02 — copy the token pipeline VERBATIM so the two sides match)
    - .planning/phases/06-spec-gate/06-CONTEXT.md (locked: 3 fixed questions, literal "confirm" gate, SPEC.md format, token algorithm, VERIFY_CMD derivation)
    - .planning/phases/06-spec-gate/06-RESEARCH.md (Pitfall 5 write SPEC.md BEFORE updating PROGRESS.md; Pitfall 6 sed | delimiter; Pattern 4/5)
  </read_first>
  <action>
Replace `skills/spec/SKILL.md` with the full skill. Keep frontmatter `name: spec`, `description:`, `argument-hint: "<task description>"`. The prose instructs Claude to:

STEP 1 — Interview. Ask exactly these three fixed questions in one message and wait for answers (use `$ARGUMENTS` as the task-description context):
  1. "What can go wrong with this task?"
  2. "What does 'done' look like — how will you know it is working?"
  3. "What is the smallest safe change that proves the core requirement?"

STEP 2 — Propose draft. Compose a draft SPEC.md in the exact interfaces format: `## Risk List` from Q1; `## Acceptance Criteria` (numbered, >=1 criterion) from Q2/Q3; `## Verify Command` prose note. Show the draft INLINE. Write NO file yet. In the draft frontmatter use `confirm-token: PENDING` and state the token is computed only at confirm time. Also show the derived VERIFY_CMD (STEP 6) so the human sees it before confirming.

STEP 3 — Confirm gate. State explicitly: "Type the literal word confirm to write this spec. Any other response (edits, 'yes', 'ok', 'looks good') will NOT write it." If the human types anything other than exactly `confirm`, incorporate edits and re-propose (back to STEP 2). Only the literal string `confirm` proceeds. Auto-confirm is prohibited.

STEP 4 — On "confirm", guard and prepare (bash):
```
PROGRESS_FILE="${PWD}/.progress/PROGRESS.md"
SPEC_FILE="${PWD}/.progress/SPEC.md"
if [ ! -f "$PROGRESS_FILE" ]; then
  echo "PROGRESS.md missing — run the SessionStart bootstrap or create .progress/PROGRESS.md first" >&2
  exit 1
fi
TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
```
Then WRITE `.progress/SPEC.md` with the Write tool (this path is exempt in stub-reject) using frontmatter `task:`, `confirm-token: PENDING`, `confirmed-at: <TIMESTAMP>`, plus the confirmed `## Risk List` / `## Acceptance Criteria` / `## Verify Command` sections.

STEP 5 — Compute token from the WRITTEN file and patch it in (guarantees byte-identity with stub-reject):
```
TOKEN=$(awk '/^## Acceptance Criteria$/{in_sec=1;next} in_sec && /^## /{exit} in_sec{print}' "$SPEC_FILE" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//' | shasum -a 256 | cut -d' ' -f1)
sed -i.bak "s|^confirm-token: PENDING|confirm-token: ${TOKEN}|" "$SPEC_FILE" && rm -f "${SPEC_FILE}.bak"
```
Patching the frontmatter token line does NOT change the `## Acceptance Criteria` section, so the token stays valid when stub-reject recomputes it.

STEP 6 — Derive VERIFY_CMD from the FIRST criterion (GATE-04) and update PROGRESS.md IN PLACE via sed (Pitfall 5 — SPEC.md is already written; sed is the established pattern and avoids the Write/Edit hook path):
```
FIRST_CRITERION=$(awk '/^## Acceptance Criteria$/{in_sec=1;next} in_sec && /^## /{exit} in_sec && NF{print; exit}' "$SPEC_FILE" | sed 's/^[[:space:]]*[0-9]*[.)]*[[:space:]]*//;s/^[[:space:]]*[-*][[:space:]]*//;s/^[[:space:]]*//;s/[[:space:]]*$//')
DERIVED_CMD="$FIRST_CRITERION"
sed -i.bak "s|^VERIFY_CMD:.*|VERIFY_CMD: ${DERIVED_CMD}|" "$PROGRESS_FILE" && rm -f "${PROGRESS_FILE}.bak"
```
Note in prose: if the first criterion is not directly runnable, the human may hand-edit PROGRESS.md VERIFY_CMD afterward (Open Question 3).

STEP 7 — Report the written SPEC.md path, the confirm-token, and the new VERIFY_CMD. Tell the human the spec gate is now open for this task.

Hard constraints to encode in prose:
- `shasum -a 256` only — never the Linux-only variant (Pitfall 3).
- The token pipeline must be copied verbatim from stub-reject.sh so both sides hash identical bytes (Pitfall 2).
- No file is written before the literal `confirm` (GATE-01 invariant).
- Write SPEC.md BEFORE touching PROGRESS.md (Pitfall 5).
- The skill file itself must contain no bare stub marker line (a line that is only the word pass, an uppercase not-done marker, or a not-implemented marker) — stub-reject would flag its own deploy otherwise.
  </action>
  <verify>
    <automated>grep -q 'shasum -a 256' /Users/oanhduong/Work/mine/build-anything/skills/spec/SKILL.md && grep -qi 'confirm' /Users/oanhduong/Work/mine/build-anything/skills/spec/SKILL.md && test $(grep -c 'sha256sum' /Users/oanhduong/Work/mine/build-anything/skills/spec/SKILL.md) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `test -f skills/spec/SKILL.md` and `grep -q '^name: spec$' skills/spec/SKILL.md`
    - Three interview questions present: `grep -qF 'What can go wrong' skills/spec/SKILL.md`, `grep -qF "What does 'done' look like" skills/spec/SKILL.md`, `grep -qF 'smallest safe change' skills/spec/SKILL.md`
    - Confirm gate present: `grep -qF 'literal word confirm' skills/spec/SKILL.md` (or equivalent phrasing requiring the literal string confirm)
    - Token pipeline present and identical to stub-reject: `grep -qF 'shasum -a 256' skills/spec/SKILL.md` and `grep -qF 'awk ' skills/spec/SKILL.md`
    - `grep -c 'sha256sum' skills/spec/SKILL.md` returns 0 (Linux-only tool absent)
    - VERIFY_CMD derivation present: `grep -qF 'VERIFY_CMD:' skills/spec/SKILL.md` and `grep -qF 'sed -i.bak' skills/spec/SKILL.md`
    - PROGRESS.md-missing guard present: `grep -qF 'PROGRESS.md missing' skills/spec/SKILL.md`
    - `grep -cE '^[[:space:]]*pass[[:space:]]*$|[T]ODO|[N]otImplemented' skills/spec/SKILL.md` returns 0 (skill file has no stub markers)
  </acceptance_criteria>
  <done>skills/spec/SKILL.md is the full /spec skill: 3 fixed questions, propose-then-confirm gate on the literal "confirm", shasum -a 256 token identical to stub-reject, SPEC.md written before PROGRESS.md, VERIFY_CMD derived from the first criterion.</done>
</task>

<task type="auto">
  <name>Task 2: Automated round-trip guard — a skill-shaped SPEC.md is accepted by stub-reject (Binary F)</name>
  <files>scripts/test-spec-gate.sh</files>
  <read_first>
    - skills/spec/SKILL.md (the token + SPEC.md write logic just implemented)
    - hooks/stub-reject.sh (the token re-derivation the round-trip must satisfy)
    - scripts/test-spec-gate.sh (Binary D already proves the canonical-pipeline token is accepted; this task confirms the multi-section skill shape round-trips)
  </read_first>
  <action>
Append a new case (Binary F) to `scripts/test-spec-gate.sh` proving the skill's write-then-patch procedure produces a token stub-reject accepts on a realistic multi-section SPEC.md. In a fresh `mktemp -d` with `.progress/`:
1. Write a PROGRESS.md with `VERIFY_CMD: exit 0`, `CURRENT_TASK: roundtrip`, `BLOCKED_COUNT: 0` (Pitfall 7).
2. Reproduce the skill STEP 4-5 procedure exactly: write `$TMP/.progress/SPEC.md` with `confirm-token: PENDING` and a `## Acceptance Criteria` section (>=2 numbered criteria) FOLLOWED BY a `## Verify Command` section (so the awk boundary `/^## /{exit}` is exercised and the token covers only the criteria section). Compute `TOKEN` via the CANONICAL pipeline, then `sed -i.bak` patch `confirm-token: PENDING` to the token.
3. Send a Write MOCK to `src/foo.sh` (content `echo hi\n`) through stub-reject; assert exit 0 (gate passes) — proves the multi-section SPEC.md round-trips.
4. Then tamper (sed-swap one criterion) and send the same Write; assert exit 2 with "SPEC.md token invalid".
Keep the same pass()/fail() helpers and the final `[ "$FAIL" -eq 0 ]`. Binary F must be GREEN once plan 02's stub-reject changes are in place.
  </action>
  <verify>
    <automated>bash /Users/oanhduong/Work/mine/build-anything/scripts/test-spec-gate.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF 'Binary F' scripts/test-spec-gate.sh` (round-trip case present)
    - `grep -qF '## Verify Command' scripts/test-spec-gate.sh` (multi-section SPEC.md exercised so the awk `/^## /{exit}` boundary is tested)
    - `bash scripts/test-spec-gate.sh` exits 0 (all binaries including F GREEN)
    - `bash scripts/test-enforcement.sh && bash scripts/test-verifier-independence.sh` exit 0 (no regression)
  </acceptance_criteria>
  <done>test-spec-gate.sh Binary F proves a multi-section, skill-shaped SPEC.md with a patched token is accepted by stub-reject and rejected after criteria tampering; full suite green.</done>
</task>

</tasks>

<verification>
- `grep` acceptance checks on skills/spec/SKILL.md pass (3 questions, confirm gate, identical token pipeline, VERIFY_CMD derivation, no stub markers, no sha256sum).
- `bash scripts/test-spec-gate.sh` exits 0 including the new Binary F round-trip.
- `bash scripts/test-enforcement.sh && bash scripts/test-verifier-independence.sh` exit 0.
</verification>

<success_criteria>
GATE-01 and GATE-04 are satisfied structurally: /spec runs the 3-question interview, writes SPEC.md only after the literal "confirm", produces a token stub-reject accepts (round-trip proven), and derives VERIFY_CMD from the first criterion into PROGRESS.md.
</success_criteria>

<output>
After completion, create `.planning/phases/06-spec-gate/06-03-SUMMARY.md`
</output>
