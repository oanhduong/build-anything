---
phase: 06-spec-gate
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/test-spec-gate.sh
  - skills/spec/SKILL.md
autonomous: true
requirements: [GATE-02, GATE-03]
must_haves:
  truths:
    - "Running test-spec-gate.sh against the current (unmodified) stub-reject.sh exits non-zero (RED anchor) because Binary A/B/C fail"
    - "test-spec-gate.sh asserts the four blocking/passing behaviors: absent, unconfirmed, tampered, and valid-passes"
    - "test-spec-gate.sh asserts the .progress/SPEC.md self-write exemption (writing SPEC.md itself is never gated)"
    - "skills/spec/SKILL.md exists as a valid skill file so install.sh globs skills/spec/"
  artifacts:
    - path: "scripts/test-spec-gate.sh"
      provides: "Binary A/B/C/D/E assertions for the spec gate; RED at wave 0, GREEN after wave 1"
      contains: "SPEC.md absent"
      min_lines: 90
    - path: "skills/spec/SKILL.md"
      provides: "/spec skill scaffold with valid frontmatter (full interview logic lands in wave 2)"
      contains: "name: spec"
      min_lines: 8
  key_links:
    - from: "scripts/test-spec-gate.sh"
      to: "hooks/stub-reject.sh"
      via: "pipe MOCK_JSON to STUB_REJECT, capture exit code + stderr"
      pattern: "STUB_REJECT"
---

<objective>
Create the TDD RED anchor for the Phase 6 spec gate and the /spec skill scaffold.

`scripts/test-spec-gate.sh` encodes the phase's binary exit criteria (A/B/C) plus a happy-path guard (D) and the self-write exemption (E). Against the current unmodified `hooks/stub-reject.sh` the suite MUST exit non-zero — A/B/C fail because the SPEC gate does not exist yet. Wave 1 (plan 02) makes A/B/C GREEN; D/E stay GREEN throughout.

`skills/spec/SKILL.md` is created as a minimal valid skill file so `install.sh` (which globs `skills/*/`) deploys it. The full risk-driven interview + token logic is implemented in wave 2 (plan 03).

Purpose: Anchor executable pass/fail criteria before any hook is modified. No implementation can be graded without this test.
Output: `scripts/test-spec-gate.sh` (RED), `skills/spec/SKILL.md` (scaffold).
</objective>

<execution_context>
@/Users/oanhduong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oanhduong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-spec-gate/06-CONTEXT.md
@.planning/phases/06-spec-gate/06-RESEARCH.md
@.planning/phases/06-spec-gate/06-VALIDATION.md

<interfaces>
Existing test harness pattern the executor MUST mirror. Extracted from scripts/test-verifier-independence.sh.

Test file skeleton (bash, POSIX, exit 0 iff all pass):
```bash
#!/usr/bin/env bash
set -uo pipefail
HARNESS_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
STUB_REJECT="${HARNESS_DIR}/hooks/stub-reject.sh"
PASS=0; FAIL=0
pass() { echo "[PASS] $1"; PASS=$((PASS + 1)); }
fail() { echo "[FAIL] $1" >&2; FAIL=$((FAIL + 1)); }
# ... per-case blocks ...
echo "Results: $PASS passed, $FAIL failed"
[ "$FAIL" -eq 0 ]
```

Invoking stub-reject from a temp CWD (from test-verifier-independence.sh Binary B):
```bash
TMP=$(mktemp -d); mkdir -p "$TMP/.progress"
MOCK='{"tool_name":"Write","tool_input":{"path":"src/foo.sh","content":"echo hi\n"}}'
ERRFILE=$(mktemp)
cd "$TMP"; echo "$MOCK" | "$STUB_REJECT" >/dev/null 2>"$ERRFILE"; EXITCODE=$?; cd - >/dev/null
ERRTXT=$(cat "$ERRFILE"); rm -f "$ERRFILE"; rm -rf "$TMP"
```

CANONICAL token pipeline (use verbatim to build the valid SPEC.md in Binary C and D — MUST match what stub-reject will compute in plan 02):
```bash
awk '/^## Acceptance Criteria$/{in_sec=1;next} in_sec && /^## /{exit} in_sec{print}' "$SPEC_FILE" \
  | sed 's/^[[:space:]]*//;s/[[:space:]]*$//' | shasum -a 256 | cut -d' ' -f1
```

Existing skill frontmatter format (from skills/retro/SKILL.md):
```markdown
---
name: retro
description: <one-line description>
---
<prose body>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/test-spec-gate.sh with Binary A/B/C/D/E (RED anchor)</name>
  <files>scripts/test-spec-gate.sh</files>
  <read_first>
    - scripts/test-verifier-independence.sh (copy the harness skeleton, pass/fail helpers, mktemp+MOCK+cd pattern EXACTLY)
    - hooks/stub-reject.sh (understand current behavior: no SPEC gate yet, PLAN-01 fires only when VERIFY_CMD empty)
    - .planning/phases/06-spec-gate/06-RESEARCH.md (Pitfall 7: every test must set VERIFY_CMD: exit 0 in PROGRESS.md so PLAN-01 does not intercept before the SPEC gate)
  </read_first>
  <action>
Create `scripts/test-spec-gate.sh`, executable (`chmod +x`), header `#!/usr/bin/env bash` and `set -uo pipefail`. Resolve `HARNESS_DIR` and `STUB_REJECT="${HARNESS_DIR}/hooks/stub-reject.sh"`. Use the same `pass()`/`fail()` counter helpers as test-verifier-independence.sh. End with `echo "Results: $PASS passed, $FAIL failed"` and `[ "$FAIL" -eq 0 ]`.

For EVERY case: create `TMP=$(mktemp -d); mkdir -p "$TMP/.progress"` and write a PROGRESS.md containing exactly these machine lines (Pitfall 7 — VERIFY_CMD non-empty so PLAN-01 does not fire first):
```
CURRENT_TASK: spec-gate-test
VERIFY_CMD: exit 0
BLOCKED_COUNT: 0

## CURRENT STATE

## HISTORY LOG
```
Run stub-reject from inside the temp dir: `cd "$TMP"; echo "$MOCK" | "$STUB_REJECT" >/dev/null 2>"$ERRFILE"; EXITCODE=$?; cd - >/dev/null`. Capture `ERRTXT=$(cat "$ERRFILE")`. Clean up TMP + ERRFILE at the end of each case.

Implement these five cases:

Binary A — SPEC.md absent then blocked "SPEC.md absent":
- Do NOT create `$TMP/.progress/SPEC.md`.
- `MOCK='{"tool_name":"Write","tool_input":{"path":"src/foo.sh","content":"echo hi\n"}}'`
- Assert: `[ "$EXITCODE" -eq 2 ] && echo "$ERRTXT" | grep -qF "SPEC.md absent"` then pass, else fail.

Binary B — SPEC.md present with criteria but no confirm-token then blocked "SPEC.md unconfirmed":
- Write `$TMP/.progress/SPEC.md` with a `## Acceptance Criteria` section (numbered list, >=1 item) and NO `confirm-token:` line. Example body:
```
---
task: b-test
---

## Acceptance Criteria
1. test -f src/foo.sh
```
- Same Write MOCK to `src/foo.sh`.
- Assert: `[ "$EXITCODE" -eq 2 ] && echo "$ERRTXT" | grep -qF "SPEC.md unconfirmed"` then pass, else fail.

Binary C — valid token then criteria tampered then blocked "SPEC.md token invalid":
- Build a valid SPEC.md: write the file body with criteria and frontmatter `confirm-token: PENDING`, then compute the token from the written file using the CANONICAL pipeline (SPEC_FILE="$TMP/.progress/SPEC.md"): `TOKEN=$(awk '/^## Acceptance Criteria$/{in_sec=1;next} in_sec && /^## /{exit} in_sec{print}' "$SPEC_FILE" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//' | shasum -a 256 | cut -d' ' -f1)`. Patch it in: `sed -i.bak "s|^confirm-token: PENDING|confirm-token: ${TOKEN}|" "$SPEC_FILE" && rm -f "${SPEC_FILE}.bak"`.
- THEN tamper: `sed -i.bak "s|test -f src/foo.sh|test -f src/CHANGED.sh|" "$SPEC_FILE" && rm -f "${SPEC_FILE}.bak"` (alters criteria text without recomputing token).
- Same Write MOCK to `src/foo.sh`.
- Assert: `[ "$EXITCODE" -eq 2 ] && echo "$ERRTXT" | grep -qF "SPEC.md token invalid"` then pass, else fail.

Binary D — valid confirmed SPEC.md then Write NOT blocked by the spec gate (happy-path / normalization-consistency guard):
- Build a valid SPEC.md the same way as Binary C (write with `confirm-token: PENDING`, compute via CANONICAL pipeline, patch token) but do NOT tamper afterward.
- Same Write MOCK to `src/foo.sh` (content `echo hi\n` — plain, contains no stub markers so the stub-pattern check stays clean).
- Assert: `[ "$EXITCODE" -eq 0 ]` then pass, else fail. (Proves the token the CANONICAL pipeline produces is accepted by stub-reject — guards Pitfall 2.)

Binary E — Write targeting .progress/SPEC.md itself is exempt from the gate even when no SPEC.md exists:
- Do NOT create SPEC.md.
- `MOCK='{"tool_name":"Write","tool_input":{"path":".progress/SPEC.md","content":"## Acceptance Criteria\n1. x\n"}}'`
- Assert: `[ "$EXITCODE" -eq 0 ]` then pass, else fail. (Proves the self-blocking exemption; guards Pitfall 1.)

At wave 0 (unmodified stub-reject) A/B/C MUST fail and D/E pass, so the suite exits non-zero overall.
  </action>
  <verify>
    <automated>bash /Users/oanhduong/Work/mine/build-anything/scripts/test-spec-gate.sh; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/test-spec-gate.sh` (file is executable)
    - `grep -qF 'SPEC.md absent' scripts/test-spec-gate.sh`
    - `grep -qF 'SPEC.md unconfirmed' scripts/test-spec-gate.sh`
    - `grep -qF 'SPEC.md token invalid' scripts/test-spec-gate.sh`
    - `grep -qF 'shasum -a 256' scripts/test-spec-gate.sh` (canonical token pipeline present, not the Linux-only tool)
    - `grep -qF '.progress/SPEC.md' scripts/test-spec-gate.sh` (Binary E exemption case present)
    - `grep -qF 'VERIFY_CMD: exit 0' scripts/test-spec-gate.sh` (Pitfall 7 guard present in every case's PROGRESS.md)
    - Running `bash scripts/test-spec-gate.sh` exits NON-ZERO (RED) against the current stub-reject.sh
    - `grep -c 'sha256sum' scripts/test-spec-gate.sh` returns 0 (Linux-only hashing tool must NOT appear)
  </acceptance_criteria>
  <done>test-spec-gate.sh exists, is executable, encodes Binary A/B/C/D/E, and exits non-zero against the unmodified stub-reject.sh (RED anchor established).</done>
</task>

<task type="auto">
  <name>Task 2: Create skills/spec/SKILL.md scaffold</name>
  <files>skills/spec/SKILL.md</files>
  <read_first>
    - skills/retro/SKILL.md (skill frontmatter format: --- name / description --- then prose body)
    - .planning/phases/06-spec-gate/06-CONTEXT.md (the /spec behavior this scaffold will grow into — 3 questions, confirm gate, token, VERIFY_CMD derivation)
  </read_first>
  <action>
Create `skills/spec/SKILL.md` with valid frontmatter so install.sh's `skills/*/` glob deploys it. Content must be a legitimate (if incomplete) skill file — it MUST NOT contain any of the stub markers stub-reject.sh rejects (its grep pattern is `^\s*pass\s*$|[T]ODO|[N]otImplemented` — i.e. a line that is only the word pass, the uppercase not-done marker, or the not-implemented marker).

Frontmatter:
```
---
name: spec
description: Risk-driven spec interview. Asks three risk questions, proposes a draft SPEC.md, waits for explicit human "confirm", then computes a sha256 confirm-token over the acceptance criteria and writes .progress/SPEC.md plus PROGRESS.md VERIFY_CMD.
argument-hint: "<task description>"
---
```
Body (prose placeholder — the full interview + token flow is implemented in wave 2 / plan 03):
- One paragraph stating the skill's purpose: gate execution behind a human-confirmed spec with a valid confirm-token.
- A short "Flow (implemented in wave 2)" list naming the six steps: (1) ask 3 risk questions, (2) propose draft SPEC.md, (3) wait for the literal word "confirm", (4) compute `shasum -a 256` of the `## Acceptance Criteria` section, (5) write `.progress/SPEC.md` with the token, (6) derive VERIFY_CMD from the first criterion and update `.progress/PROGRESS.md`.
Keep the body prose clean of any bare stub marker line.
  </action>
  <verify>
    <automated>test -f /Users/oanhduong/Work/mine/build-anything/skills/spec/SKILL.md && grep -q '^name: spec' /Users/oanhduong/Work/mine/build-anything/skills/spec/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f skills/spec/SKILL.md`
    - `grep -q '^name: spec$' skills/spec/SKILL.md`
    - `grep -q '^description:' skills/spec/SKILL.md`
    - `grep -qi 'confirm' skills/spec/SKILL.md` (confirm gate mentioned)
    - `grep -cE '^[[:space:]]*pass[[:space:]]*$|[T]ODO|[N]otImplemented' skills/spec/SKILL.md` returns 0 (no stub markers stub-reject would flag)
  </acceptance_criteria>
  <done>skills/spec/SKILL.md exists with valid `name: spec` frontmatter and a description; install.sh will glob-deploy skills/spec/; no stub markers present.</done>
</task>

</tasks>

<verification>
- `bash scripts/test-spec-gate.sh` exits non-zero (RED) — Binary A/B/C fail against unmodified stub-reject; D/E pass.
- `bash scripts/test-enforcement.sh && bash scripts/test-verifier-independence.sh` still green (no regression — this plan only adds files).
- `skills/spec/SKILL.md` present and valid.
</verification>

<success_criteria>
test-spec-gate.sh is the executable RED anchor for the spec gate (exits non-zero now, will exit zero after plan 02), and the /spec skill scaffold exists for install deployment.
</success_criteria>

<output>
After completion, create `.planning/phases/06-spec-gate/06-01-SUMMARY.md`
</output>
