---
phase: 05-verifier-independence
plan: 04
type: execute
wave: 3
depends_on: [05-03]
files_modified:
  - settings.json
autonomous: true
requirements: [VERIF-01, VERIF-02, VERIF-03]

must_haves:
  truths:
    - "settings.json PostToolUse array includes verdicts-capture.sh with no matcher (all tools)"
    - "install.sh copies verdicts-capture.sh to ~/.claude/hooks/ and merges settings.json entry"
    - "bash scripts/test-enforcement.sh passes all ENFC checks for verdicts-capture.sh"
    - "bash scripts/test-verifier-independence.sh exits 0 from the installed hooks"
    - "dedup logic in install.sh prevents duplicate verdicts-capture.sh entry on re-run"
  artifacts:
    - path: "settings.json"
      provides: "verdicts-capture.sh wired as PostToolUse all-tools hook"
      contains: "verdicts-capture.sh"
  key_links:
    - from: "settings.json"
      to: "hooks/verdicts-capture.sh"
      via: "PostToolUse hook command entry (no matcher = all tools)"
      pattern: "verdicts-capture.sh"
    - from: "install.sh"
      to: "settings.json"
      via: "jq dedup merge — keys on (matcher + command)"
      pattern: "dedup"
---

<objective>
Wire verdicts-capture.sh into settings.json as a PostToolUse all-tools hook. Run install.sh to deploy to ~/.claude. Validate the full test suite passes from installed hooks.

Purpose: This is the final integration step. After this plan, verdicts-capture.sh fires automatically in live Claude Code sessions on every tool call. Phase 5 is shippable.
Output: settings.json updated, installed to ~/.claude, full test suite green.
</objective>

<execution_context>
@/Users/oanhduong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oanhduong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-verifier-independence/05-CONTEXT.md
@.planning/phases/05-verifier-independence/05-03-SUMMARY.md

<interfaces>
<!-- Current settings.json PostToolUse array for reference. Extracted from codebase. -->

Current settings.json PostToolUse array:
```json
"PostToolUse": [
  {
    "matcher": "Write|Edit",
    "hooks": [{"type": "command", "command": "bash ~/.claude/hooks/progress-after-edit.sh"}]
  },
  {
    "matcher": "Write|Edit",
    "hooks": [{"type": "command", "command": "bash ~/.claude/hooks/lessons-post-write.sh"}]
  },
  {
    "matcher": "Bash",
    "hooks": [{"type": "command", "command": "bash ~/.claude/hooks/lessons-on-error.sh"}]
  },
  {
    "hooks": [{"type": "command", "command": "bash ~/.claude/hooks/trace.sh"}]
  }
]
```

New entry to add (no `matcher` field = fires on ALL tool calls, same pattern as trace.sh):
```json
{
  "hooks": [{"type": "command", "command": "bash ~/.claude/hooks/verdicts-capture.sh"}]
}
```

install.sh dedup logic (lines 62-73) — keys on `(matcher, command)`:
```bash
jq -s '
  def dedup: unique_by((.matcher // "") + "|" + (.hooks | map(.command) | join(",")));
  ...
  PostToolUse:  (($existing.hooks.PostToolUse  // []) + ($harness.hooks.PostToolUse  // []) | dedup),
  ...
'
```
Adding verdicts-capture.sh entry with no matcher → key = `""|"bash ~/.claude/hooks/verdicts-capture.sh"` → safe to re-run install.sh.

install.sh copies hooks: line 23 `cp "${HARNESS_DIR}/hooks/"*.sh "${CLAUDE_DIR}/hooks/"` + line 24 `chmod +x`
install.sh copies scripts: line 44 `cp "${HARNESS_DIR}/scripts/"*.sh "${CLAUDE_DIR}/scripts/"`
Both are full overwrites — verdicts-capture.sh and test-verifier-independence.sh will be deployed automatically.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire verdicts-capture.sh in settings.json PostToolUse array</name>
  <read_first>
    - settings.json (current full content — must understand existing PostToolUse array structure before editing)
    - install.sh (lines 58-79: dedup merge logic — verify the new entry will survive re-runs without duplicates)
    - .planning/phases/05-verifier-independence/05-RESEARCH.md (Pattern 5: all-tools PostToolUse entry format)
  </read_first>
  <files>settings.json</files>
  <action>
Edit `settings.json` to add verdicts-capture.sh as a PostToolUse all-tools hook entry.

**Where to insert:** In the `"PostToolUse"` array, add the new entry BEFORE the existing trace.sh entry (which also has no matcher). This preserves trace.sh as the final PostToolUse hook (tracing comes last).

**The new entry** (no `matcher` field = fires on all tool calls):
```json
{
  "hooks": [
    {
      "type": "command",
      "command": "bash ~/.claude/hooks/verdicts-capture.sh"
    }
  ]
}
```

After the edit, the PostToolUse array must contain:
1. `{"matcher": "Write|Edit", ...progress-after-edit.sh...}`
2. `{"matcher": "Write|Edit", ...lessons-post-write.sh...}`
3. `{"matcher": "Bash", ...lessons-on-error.sh...}`
4. NEW: `{...verdicts-capture.sh...}` (no matcher)
5. `{...trace.sh...}` (no matcher — unchanged, stays last)

Use the Edit tool with a targeted replacement to insert the new entry. Do NOT restructure or reformat the rest of settings.json.

Verify the JSON is valid after editing: `jq . settings.json` must exit 0.
  </action>
  <verify>
    <automated>jq . settings.json > /dev/null && jq -r '.hooks.PostToolUse[] | .hooks[].command' settings.json | grep -q "verdicts-capture.sh"</automated>
  </verify>
  <acceptance_criteria>
    - `jq . settings.json` exits 0 (valid JSON)
    - `jq -r '.hooks.PostToolUse[] | .hooks[].command' settings.json | grep -q "verdicts-capture.sh"` exits 0
    - `jq -r '.hooks.PostToolUse[] | select(.matcher == null or .matcher == "") | .hooks[].command' settings.json | grep -q "trace.sh"` exits 0 (trace.sh still present)
    - `jq -r '.hooks.PostToolUse[] | select(.matcher == null or .matcher == "") | .hooks[].command' settings.json | grep -q "verdicts-capture.sh"` exits 0 (new entry present with no matcher)
    - `jq -r '[.hooks.PostToolUse[] | select(.matcher == "Write|Edit")] | length' settings.json` outputs 2 (existing Write|Edit entries unchanged)
    - `jq -r '[.hooks.PostToolUse[] | .hooks[].command] | index("bash ~/.claude/hooks/verdicts-capture.sh") < index("bash ~/.claude/hooks/trace.sh")' settings.json` outputs true (verdicts-capture before trace)
  </acceptance_criteria>
  <done>settings.json PostToolUse array includes verdicts-capture.sh with no matcher (all tools); valid JSON; trace.sh still last</done>
</task>

<task type="auto">
  <name>Task 2: Run install.sh and validate full test suite</name>
  <read_first>
    - install.sh (understand: hooks/*.sh → ~/.claude/hooks/ with chmod +x; scripts/*.sh → ~/.claude/scripts/; settings.json merge with dedup)
    - scripts/test-enforcement.sh (understand: pre-step runs install.sh before ENFC checks; verdicts-capture.sh now in NON_BLOCKING list)
  </read_first>
  <files>scripts/test-enforcement.sh</files>
  <action>
Run the following steps in order. Each must succeed before the next.

**Step 1: Install harness to ~/.claude**
```bash
bash install.sh
```
Expected output: "[OK] Hook scripts installed and chmod +x", "[OK] settings.json merged", "=== Install complete ===", preflight checks all pass.

Verify after install:
- `test -f ~/.claude/hooks/verdicts-capture.sh` exits 0
- `grep -q "verdicts-capture.sh" ~/.claude/settings.json` exits 0
- `test -f ~/.claude/scripts/test-verifier-independence.sh` exits 0

**Step 2: Run Phase 5 test suite**
```bash
bash scripts/test-verifier-independence.sh
```
Expected: all 6 tests pass, "Results: 6 passed, 0 failed", exit 0.

If any test fails, diagnose from the [FAIL] line output and fix before Step 3.

**Step 3: Run full enforcement test suite**
```bash
bash scripts/test-enforcement.sh
```
Expected: all ENFC checks pass including:
- "ENFC-02: verdicts-capture.sh has # tag: annotation" → [PASS]
- "ENFC-03: verdicts-capture.sh has no blocking messages (non-blocking hook, exempt)" → [PASS]
- "ENFC-04: no language-specific binaries in verdicts-capture.sh" → [PASS]
- "Results: N passed, 0 failed", exit 0

**Step 4: Verify dedup — re-run install.sh does not duplicate verdicts-capture.sh**
```bash
bash install.sh > /dev/null 2>&1
COUNT=$(jq '[.hooks.PostToolUse[] | .hooks[].command | select(contains("verdicts-capture.sh"))] | length' ~/.claude/settings.json)
echo "verdicts-capture.sh count in ~/.claude/settings.json: $COUNT"
[ "$COUNT" -eq 1 ] && echo "[PASS] dedup: no duplicate entry" || echo "[FAIL] dedup: $COUNT entries found"
```
Expected: count = 1.
  </action>
  <verify>
    <automated>bash scripts/test-verifier-independence.sh && bash scripts/test-enforcement.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f ~/.claude/hooks/verdicts-capture.sh` exits 0 after install
    - `test -x ~/.claude/hooks/verdicts-capture.sh` exits 0 (chmod +x applied by install.sh)
    - `grep -q "verdicts-capture.sh" ~/.claude/settings.json` exits 0
    - `test -f ~/.claude/scripts/test-verifier-independence.sh` exits 0
    - `bash scripts/test-verifier-independence.sh` exits 0 (all 6 tests pass)
    - `bash scripts/test-enforcement.sh` exits 0 (all ENFC checks pass)
    - `bash scripts/test-enforcement.sh 2>&1 | grep "verdicts-capture.sh" | grep -qv "\[FAIL\]"` exits 0 (no FAIL for verdicts-capture.sh)
    - `bash scripts/force-loop-test.sh` exits 0 (LOOP-01/02 unbroken after install)
    - `bash scripts/no-verify-cmd-test.sh` exits 0 (PLAN-01 unbroken after install)
    - Second install run: `jq '[.hooks.PostToolUse[] | .hooks[].command | select(contains("verdicts-capture.sh"))] | length' ~/.claude/settings.json` outputs 1
  </acceptance_criteria>
  <done>install.sh deploys all Phase 5 artifacts to ~/.claude; all 6 Phase 5 tests pass; all ENFC checks pass; no duplicate hooks on re-install</done>
</task>

</tasks>

<verification>
Phase 5 complete when:
- `bash scripts/test-verifier-independence.sh` exits 0 (Binary A, Binary B, VERIF-01 pass, VERIF-01 fail, VERIF-02, VERIF-03 all GREEN)
- `bash scripts/test-enforcement.sh` exits 0 (full ENFC suite green)
- `bash scripts/force-loop-test.sh` exits 0 (LOOP-01/02 preserved)
- `bash scripts/no-verify-cmd-test.sh` exits 0 (PLAN-01 preserved)
- `grep -q "verdicts-capture.sh" ~/.claude/settings.json` exits 0

Manual verification recommended (not automated — requires live session):
- Create toy SPEC.md with 2 criteria, ensure VERDICTS.md absent
- Run `echo '{"stop_hook_active":false,"cwd":"<path>"}' | bash ~/.claude/hooks/stop-hook.sh`
- Observe: exit 2, stderr contains both criterion strings (real run proof criterion A from ROADMAP.md)
</verification>

<success_criteria>
settings.json wired. install.sh deploys verdicts-capture.sh to ~/.claude/hooks/. Full test suite green. dedup verified. Phase 5 Verifier Independence ships.
</success_criteria>

<output>
After completion, create `.planning/phases/05-verifier-independence/05-04-SUMMARY.md`
</output>
