---
phase: 01-enforcement-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - failure-lib/eval-subshell.md
  - failure-lib/openpyxl-engine.md
  - failure-lib/dotenv-module-scope.md
  - failure-lib/mock-import-boundary.md
  - failure-lib/static-test-fixture.md
  - failure-lib/home-scope.md
autonomous: true
requirements: [ENFC-01]

must_haves:
  truths:
    - "Every Phase 0 failure category has a machine-readable entry in failure-lib/"
    - "Architecture failures carry no model-version field"
    - "Model-crutch failures carry model-version: claude-sonnet-4-6"
    - "All entries include a How to Fix section and a Verifier Instruction section"
  artifacts:
    - path: "failure-lib/eval-subshell.md"
      provides: "F-EVAL-SUBSHELL documented entry (architecture, hook)"
      contains: "enforcement-type: hook"
    - path: "failure-lib/openpyxl-engine.md"
      provides: "F-OPENPYXL-ENGINE documented entry (model-crutch, verifier-check)"
      contains: "enforcement-type: verifier-check"
    - path: "failure-lib/dotenv-module-scope.md"
      provides: "F-DOTENV-SCOPE documented entry (model-crutch, verifier-check)"
      contains: "model-version: claude-sonnet-4-6"
    - path: "failure-lib/mock-import-boundary.md"
      provides: "F-MOCK-IMPORT-BOUNDARY documented entry (model-crutch, verifier-check)"
      contains: "model-version: claude-sonnet-4-6"
    - path: "failure-lib/static-test-fixture.md"
      provides: "F-STATIC-FIXTURE documented entry (architecture, verifier-check)"
      contains: "enforcement-type: verifier-check"
    - path: "failure-lib/home-scope.md"
      provides: "F-HOME-SCOPE documented entry (model-crutch, verifier-check)"
      contains: "model-version: claude-sonnet-4-6"
  key_links:
    - from: "agents/verifier.md"
      to: "failure-lib/*.md"
      via: "runtime scan instruction (added in Plan 02)"
      pattern: "enforcement-type: verifier-check"
---

<objective>
Populate failure-lib/ with one .md file per Phase 0 failure category. These entries are the machine-readable record that ENFC-01 requires — every identified failure has a corresponding documented enforcement entry, tagged correctly, with self-fix instructions and verifier guidance.

Purpose: Provide the data layer for Phase 1 enforcement. Python-specific failures (which cannot become grep hooks per ENFC-04) are enforced through the verifier agent's runtime failure-lib scan (added in Plan 02). Architecture failures document the correct pattern for hook authors.

Output: 6 `.md` files in `failure-lib/` — each with YAML frontmatter (id, tag, enforcement-type, model-version where applicable) and a structured body (What Failed, Why It Happens, How to Fix, Grep Pattern or Verifier Instruction).
</objective>

<execution_context>
@/Users/oanhduong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oanhduong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-enforcement-hardening/01-CONTEXT.md
@.planning/phases/01-enforcement-hardening/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write architecture-tagged failure-lib entries (eval-subshell, static-test-fixture)</name>
  <files>
    failure-lib/eval-subshell.md
    failure-lib/static-test-fixture.md
  </files>

  <read_first>
    - .planning/phases/01-enforcement-hardening/01-RESEARCH.md (complete entry content for both failures at "Code Examples" section — copy verbatim)
    - hooks/stop-hook.sh (verify the subshell eval pattern `( eval "$VERIFY_CMD" )` already exists — confirms fix is in place and this entry documents it)
  </read_first>

  <action>
Create `failure-lib/eval-subshell.md` with EXACTLY this content:

```markdown
---
id: eval-subshell
tag: architecture
enforcement-type: hook
---

## What Failed

`eval VERIFY_CMD` inside `set -euo pipefail` causes the eval's exit code to propagate into
the parent shell via `set -e`. If VERIFY_CMD exits 1, the hook exits 1 (non-blocking), not 2.
The `if eval ...; then` construct does NOT protect against this.

## Why It Happens

`set -e` treats a non-zero exit from eval as a fatal error. The if-condition suppresses -e for
the overall conditional test, but inside the conditional body, -e is still active.

## How to Fix

Wrap eval in a subshell: `( eval "$VERIFY_CMD" )`. The subshell exit code is captured by the
parent as a variable, without triggering -e in the parent shell.

## Grep Pattern

`grep -n 'eval.*VERIFY_CMD' hooks/stop-hook.sh` — confirm match is `( eval` not bare `eval`.
```

Create `failure-lib/static-test-fixture.md` with EXACTLY this content:

```markdown
---
id: static-test-fixture
tag: architecture
enforcement-type: verifier-check
---

## What Failed

Generating test fixture files at test time (e.g., creating sample.xlsx in a fixture function)
introduces race conditions and non-reproducibility. Tests may fail due to file creation order.

## Why It Happens

Natural instinct is to generate fixtures programmatically. But test-time generation depends on
the test environment's write access, timing, and teardown order.

## How to Fix

Commit static fixture files (e.g., fixtures/sample.xlsx) to the repository. Never generate
them at test time. The fixture file is version-controlled and always reproducible.

## Verifier Instruction

When reviewing test files: check that any fixture files referenced in tests (Excel, CSV, JSON,
images) are committed to the repository, not generated by test code. Flag any conftest.py
fixture that creates files rather than pointing to committed files.
```

Note: `static-test-fixture` is tagged `architecture` (not `model-crutch`) because this rule is permanent and applies regardless of model version. No `model-version` field.
  </action>

  <verify>
    <automated>grep -q '^id: eval-subshell$' failure-lib/eval-subshell.md && grep -q '^tag: architecture$' failure-lib/eval-subshell.md && grep -q '^enforcement-type: hook$' failure-lib/eval-subshell.md && grep -q 'How to Fix' failure-lib/eval-subshell.md && ! grep -q 'model-version' failure-lib/eval-subshell.md && grep -q '^id: static-test-fixture$' failure-lib/static-test-fixture.md && grep -q '^enforcement-type: verifier-check$' failure-lib/static-test-fixture.md && grep -q 'Verifier Instruction' failure-lib/static-test-fixture.md && echo "PASS: architecture entries valid"</automated>
  </verify>

  <done>
    - failure-lib/eval-subshell.md exists; `tag: architecture`; `enforcement-type: hook`; no `model-version` field; contains "How to Fix" and "Grep Pattern" sections
    - failure-lib/static-test-fixture.md exists; `tag: architecture`; `enforcement-type: verifier-check`; no `model-version` field; contains "Verifier Instruction" section
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write model-crutch failure-lib entries (openpyxl-engine, dotenv-module-scope, mock-import-boundary, home-scope)</name>
  <files>
    failure-lib/openpyxl-engine.md
    failure-lib/dotenv-module-scope.md
    failure-lib/mock-import-boundary.md
    failure-lib/home-scope.md
  </files>

  <read_first>
    - .planning/phases/01-enforcement-hardening/01-RESEARCH.md (complete entry content for openpyxl-engine, dotenv-module-scope, mock-import-boundary at "Code Examples" section — copy verbatim; home-scope content at "Open Questions" item 3)
    - .planning/STATE.md §Decisions (confirms model version string: `claude-sonnet-4-6` — use exactly this value in all model-version fields)
  </read_first>

  <action>
Create `failure-lib/openpyxl-engine.md`:

```markdown
---
id: openpyxl-engine
tag: model-crutch
enforcement-type: verifier-check
model-version: claude-sonnet-4-6
---

## What Failed

`pd.read_excel(path)` without `engine='openpyxl'` fails on .xlsx files in pandas 1.2+.
xlrd dropped .xlsx support; the correct engine must be specified explicitly.

## Why It Happens

Model training data may include older pandas examples (pre-1.2) where engine defaulted to xlrd.
The model generates the older pattern because it appears frequently in training data.

## How to Fix

Always use `pd.read_excel(path, engine='openpyxl')` for .xlsx files.
For .xls files (legacy), use `engine='xlrd'`.

## Verifier Instruction

When reviewing Python code that reads Excel files: check that every `pd.read_excel()` call
for `.xlsx` files includes `engine='openpyxl'`. Flag any call missing this argument.
```

Create `failure-lib/dotenv-module-scope.md`:

```markdown
---
id: dotenv-module-scope
tag: model-crutch
enforcement-type: verifier-check
model-version: claude-sonnet-4-6
---

## What Failed

`load_dotenv()` called only in `main.py` entrypoint. When pytest imports the module directly,
env vars from .env are not loaded, causing `anthropic.AuthenticationError` in tests.

## Why It Happens

Model places `load_dotenv()` at the script entrypoint only. Modules that reference env vars at
import time (e.g., `client = anthropic.Anthropic()` at module scope) require load_dotenv() in
the module itself, not just in the calling script.

## How to Fix

Call `load_dotenv()` at module level in every module that reads env vars at import time.
Not just in main.py — in the module where the env var is first used.

## Verifier Instruction

When reviewing Python modules that use env vars (os.getenv, os.environ): check that
`load_dotenv()` is called at module level (not just inside a function or in main.py).
Specifically check any module that creates an API client at module scope.
```

Create `failure-lib/mock-import-boundary.md`:

```markdown
---
id: mock-import-boundary
tag: model-crutch
enforcement-type: verifier-check
model-version: claude-sonnet-4-6
---

## What Failed

Mocking `anthropic.Anthropic` at the global namespace (`@patch('anthropic.Anthropic')`) does
not intercept a module that has already imported and bound the name at import time.

## Why It Happens

Python's unittest.mock patches the name binding in a specific namespace. If `recommend.py`
does `import anthropic` and then `client = anthropic.Anthropic()`, patching the global
`anthropic.Anthropic` may not affect the already-bound reference in recommend.py's namespace.

## How to Fix

Patch at the import location: `@patch('modules.recommend.anthropic.Anthropic')`.
The patch target must be where the name is looked up, not where it is defined.

## Verifier Instruction

When reviewing Python test files that mock external SDK clients: verify the patch target uses
the importing module's path (e.g., `modules.recommend.anthropic.Anthropic`), not the SDK's
global namespace (e.g., `anthropic.Anthropic`). Specifically check conftest.py fixtures.
```

Create `failure-lib/home-scope.md`:

```markdown
---
id: home-scope
tag: model-crutch
enforcement-type: verifier-check
model-version: claude-sonnet-4-6
---

## What Failed

`HOME=tmp echo ... | hook` only sets HOME for the `echo` process, not for the hook process
on the right side of the pipeline. The hook sees the original HOME, not the overridden value.

## Why It Happens

Environment variable prefix (`VAR=val cmd`) only applies to the directly-prefixed command.
In a pipeline (`cmd1 | cmd2`), each side is a separate process; prefix only affects `cmd1`.

## How to Fix

Use `HOME=tmp bash -c '... | hook'` to scope HOME to both sides of the pipeline inside a
single bash -c invocation. The env var applies to the entire subshell, covering all processes.

## Verifier Instruction

When reviewing bash test scripts that override HOME for pipeline commands: check that the
override uses `HOME=val bash -c '...'` form, not `HOME=val cmd | other_cmd`. Specifically
check any test script that sets HOME and pipes input to a hook.
```

CRITICAL: All four files MUST have `model-version: claude-sonnet-4-6` in YAML frontmatter. Do not omit this field for any model-crutch entry.
  </action>

  <verify>
    <automated>for f in failure-lib/openpyxl-engine.md failure-lib/dotenv-module-scope.md failure-lib/mock-import-boundary.md failure-lib/home-scope.md; do grep -q '^tag: model-crutch$' "$f" && grep -q '^model-version: claude-sonnet-4-6$' "$f" && grep -q '^enforcement-type: verifier-check$' "$f" && grep -q 'Verifier Instruction' "$f" && grep -q 'How to Fix' "$f" || { echo "FAIL: $f missing required fields"; exit 1; }; done && echo "PASS: all model-crutch entries valid"</automated>
  </verify>

  <done>
    - 4 model-crutch failure-lib entries exist; each has `tag: model-crutch`, `enforcement-type: verifier-check`, `model-version: claude-sonnet-4-6`, and body sections "How to Fix" and "Verifier Instruction"
    - `ls failure-lib/*.md | wc -l` returns 6 (all entries present after Task 1 + Task 2)
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
```bash
# All 6 entries exist
ls failure-lib/*.md | wc -l   # must be >= 6

# Architecture entries have no model-version
! grep -l 'model-version' failure-lib/eval-subshell.md failure-lib/static-test-fixture.md 2>/dev/null | grep -q .

# Model-crutch entries all have model-version: claude-sonnet-4-6
for f in failure-lib/openpyxl-engine.md failure-lib/dotenv-module-scope.md failure-lib/mock-import-boundary.md failure-lib/home-scope.md; do
  grep -q 'model-version: claude-sonnet-4-6' "$f" || echo "FAIL: $f"
done

# Every entry has a How to Fix section
grep -rL 'How to Fix' failure-lib/*.md  # must return empty

# Every verifier-check entry has a Verifier Instruction section
for f in failure-lib/*.md; do
  if grep -q 'enforcement-type: verifier-check' "$f"; then
    grep -q 'Verifier Instruction' "$f" || echo "FAIL: Verifier Instruction missing in $f"
  fi
done
```
</verification>

<success_criteria>
- 6 files in failure-lib/ (eval-subshell.md, openpyxl-engine.md, dotenv-module-scope.md, mock-import-boundary.md, static-test-fixture.md, home-scope.md)
- All have valid YAML frontmatter with id, tag, enforcement-type
- 4 model-crutch entries each carry `model-version: claude-sonnet-4-6`
- 2 architecture entries have NO model-version field
- Every entry body includes "How to Fix" section
- Every verifier-check entry includes "Verifier Instruction" section
- ENFC-01 (verifier-check path): all 6 identified Phase 0 failures are documented
</success_criteria>

<output>
After completion, create `.planning/phases/01-enforcement-hardening/01-01-SUMMARY.md`
</output>
