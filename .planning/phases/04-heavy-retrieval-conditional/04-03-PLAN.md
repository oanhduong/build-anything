---
phase: 04-heavy-retrieval-conditional
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - scripts/retrieval-e2e-test.sh
  - scripts/retrieval/fixtures/known-miss-query.txt
  - scripts/retrieval/fixtures/corpus-seed.md
  - install.sh
autonomous: false
requirements: [RETR-02]
user_setup: []

must_haves:
  truths:
    - "retrieval-e2e-test.sh proves vector search returns >=1 result for a query that grep provably missed (regression fixture)"
    - "retrieval-e2e-test.sh proves a missing/stale index degrades to grep and search still returns (never hard-fails)"
    - "retrieval-e2e-test.sh proves the context-pull search external API (argument-hint) is unchanged"
    - "retrieval-e2e-test.sh logs vector-path latency for a 100-entry corpus query"
    - "The done command ./scripts/check-retrieval-gate.sh && ./scripts/retrieval-e2e-test.sh runs green when the gate is open and chromadb is installed"
    - "install.sh notes the optional gate-gated chromadb dependency without unconditionally pip-installing it"
  artifacts:
    - path: "scripts/retrieval-e2e-test.sh"
      provides: "PASS/FAIL e2e suite (retro-e2e-test.sh pattern, mktemp isolation)"
      min_lines: 60
      contains: "Results:"
    - path: "scripts/retrieval/fixtures/known-miss-query.txt"
      provides: "A query grep misses literally but vector finds semantically (regression fixture)"
    - path: "scripts/retrieval/fixtures/corpus-seed.md"
      provides: "Seed document whose semantic terms differ from the known-miss query (so grep misses, vector hits)"
    - path: "install.sh"
      provides: "Gate-gated chromadb note (no unconditional pip install)"
      contains: "chromadb"
  key_links:
    - from: "scripts/retrieval-e2e-test.sh"
      to: "scripts/retrieval/search.py"
      via: "asserts vector hit on known-miss fixture + grep fallback on removed index"
      pattern: "search\\.py"
    - from: "scripts/retrieval-e2e-test.sh"
      to: "scripts/build-retrieval-index.sh"
      via: "builds index over the seed corpus before querying"
      pattern: "build-retrieval-index\\.sh"
---

<objective>
Build the Phase 4 done command `retrieval-e2e-test.sh` (RETR-02 e2e proof), the
regression fixtures it needs (a query grep provably misses but vector finds), and add a
gate-gated chromadb note to install.sh. Close the phase with a human checkpoint verifying
the full done command (`check-retrieval-gate.sh && retrieval-e2e-test.sh`).

GATE: This plan MUST NOT begin until Plan 04-01 reports the gate OPEN and Plan 04-02 has
landed the search/index backend. If the gate is CLOSED, defer indefinitely.

Purpose: Success criteria 2 and 3 of the ROADMAP require a proven regression (vector finds
what grep missed) and a 100-entry latency benchmark. The degraded-to-grep contract and the
unchanged-API contract must both be machine-asserted.

Output: `scripts/retrieval-e2e-test.sh`, `scripts/retrieval/fixtures/*`, install.sh note,
and a human sign-off checkpoint.
</objective>

<execution_context>
@/Users/oanhduong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oanhduong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-heavy-retrieval-conditional/04-CONTEXT.md
@.planning/phases/04-heavy-retrieval-conditional/04-RESEARCH.md
@.planning/ROADMAP.md

<interfaces>
<!-- GATE PRECHECK: first action — test -f .planning/phases/04-heavy-retrieval-conditional/gate-evidence.md; if absent, STOP. -->

From scripts/retro-e2e-test.sh — the exact test-script skeleton to mirror:
```bash
set -uo pipefail   # NOT set -e — grep no-match returns 1, must not abort
HARNESS_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PASS=0; FAIL=0
pass() { echo "[PASS] $1"; PASS=$((PASS + 1)); }
fail() { echo "[FAIL] $1" >&2; FAIL=$((FAIL + 1)); }
WORK=$(mktemp -d)            # isolation — never touch real ~/.claude/.retrieval
# ... per-check assertions ...
rm -rf "$WORK"
echo "Results: $PASS passed, $FAIL failed"
[ "$FAIL" -eq 0 ]
```

Backend contracts from Plan 02 (already built):
- `python3 scripts/retrieval/build_index.py --lib-dir DIR --docs-dir DIR --index-path DIR` → builds index, prints INDEXED=<n>, exit 3 if chromadb missing
- `python3 scripts/retrieval/search.py --query "Q" --index-path DIR --lib-dir DIR` → prints results; `BACKEND=vector` or `BACKEND=grep` to stderr; ALWAYS exit 0
- `bash scripts/build-retrieval-index.sh ...` → wraps build_index.py, never hard-fails

From install.sh — the existing scripts install block (scripts are full-overwrite + chmod +x; do NOT add unconditional pip install — RESEARCH note 10 / Open Q5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: regression fixtures + retrieval-e2e-test.sh + install.sh chromadb note</name>
  <files>scripts/retrieval-e2e-test.sh, scripts/retrieval/fixtures/known-miss-query.txt, scripts/retrieval/fixtures/corpus-seed.md, install.sh</files>
  <read_first>
    - scripts/retro-e2e-test.sh (mirror this exactly: set -uo pipefail, mktemp -d isolation, pass/fail helpers, final "Results: N passed, M failed", exit non-zero if any fail)
    - scripts/replay-giavico-failures.sh (per-check assertion structure, pre-step pattern)
    - scripts/retrieval/build_index.py and scripts/retrieval/search.py (the exact CLI contracts the e2e asserts against — from Plan 02)
    - install.sh (the existing scripts-install block to append the chromadb note near; do NOT add unconditional pip install — RESEARCH note 10)
    - .planning/phases/04-heavy-retrieval-conditional/04-RESEARCH.md (Wave 0 Gaps: regression fixture + synthetic 100-corpus needed; Pitfall 4 warm model before timing; success criterion 2 = vector finds what grep missed)
  </read_first>
  <action>
    Create the regression fixtures (these make success-criterion 2 reproducible):
    - `scripts/retrieval/fixtures/corpus-seed.md`: a single lesson-shaped doc whose BODY uses one
      vocabulary (e.g. about "credential rotation" / "token refresh" / "login session") but does NOT
      contain the literal word "authentication". Frontmatter: `id: auth-rotation`, `tags: [security]`,
      `when: on-error`, `error-match: token`. Body: "## What happened\nLogin session credentials were
      not rotated...\n## How to avoid\nRotate the login token on refresh...".
    - `scripts/retrieval/fixtures/known-miss-query.txt`: a single line containing the query
      `authentication` — a term that is SEMANTICALLY related to the seed doc but appears NOWHERE
      literally in it, so `grep -rn authentication` returns 0 results while vector search (semantic)
      returns the seed doc. This is the grep-miss / vector-hit regression pair.

    Create `scripts/retrieval-e2e-test.sh` (chmod +x), mirroring retro-e2e-test.sh exactly:
    - Header `# tag: architecture`. `set -uo pipefail`. Resolve HARNESS_DIR. PASS/FAIL counters + helpers.
    - `WORK=$(mktemp -d)` isolation; `trap 'rm -rf "$WORK"' EXIT`. Build an isolated corpus dir
      `$WORK/lib` containing the fixture corpus-seed.md plus enough generated filler `.md` files to
      reach a 100-entry corpus (for the latency benchmark of success-criterion 3). Index path `$WORK/idx`.
    - PRE-STEP: detect chromadb. Run `python3 -c "import chromadb"`; if it fails, print
      `[SKIP] chromadb not installed — vector-path checks skipped; run: pip install chromadb` and
      record a SKIP (do NOT count as FAIL, but the suite must clearly report SKIPPED vector checks).
      Always run the grep-fallback and API-unchanged checks (they need no chromadb).
    - CHECK A (grep baseline miss — proves the regression is real): run
      `grep -rn "$(cat .../known-miss-query.txt)" $WORK/lib` and assert it returns 0 results
      (the query literally misses). pass/fail accordingly.
    - CHECK B (vector recall — success criterion 2, vector-only, skipped if no chromadb): build the
      index (`bash build-retrieval-index.sh --lib-dir $WORK/lib --docs-dir $WORK/empty-docs --index-path $WORK/idx`),
      WARM the model once (a throwaway search query) to exclude cold-start from any timing (Pitfall 4),
      then run `search.py --query "<known-miss query>" --index-path $WORK/idx --lib-dir $WORK/lib` and
      assert stderr contains `BACKEND=vector` AND stdout contains the seed id `auth-rotation`. This is
      the "vector returns what grep missed" proof.
    - CHECK C (degraded-to-grep — never hard-fails): remove `$WORK/idx` entirely, run
      `search.py --query "openpyxl" --index-path $WORK/idx --lib-dir $WORK/lib`; assert exit code is 0
      AND stderr contains `BACKEND=grep`. (Runs regardless of chromadb.)
    - CHECK D (API unchanged — structural): assert
      `grep -q 'argument-hint: "search <query> | get-file <path> | expand-summary <section>"' skills/context-pull/SKILL.md`.
    - CHECK E (latency log — success criterion 3, vector-only, skipped if no chromadb): time a vector
      query over the 100-entry indexed corpus using `python3 -c` with time.perf_counter (or call
      search.py and time the call), print `vector-latency-ms=<value>` to stdout for the record. This is
      a LOG/record check (always pass if it produces a number); the gate already validated the grep
      baseline. Do not hard-assert a threshold here.
    - Final: `echo "Results: $PASS passed, $FAIL failed"`; exit non-zero iff FAIL>0. SKIPPED vector
      checks (no chromadb) must NOT cause a non-zero exit, but must be visibly reported.

    EDIT `install.sh`: near the scripts-install block, add a NON-EXECUTABLE comment block (no
    unconditional `pip install`). Exactly:
    ```bash
    # --- Phase 4 (Heavy Retrieval) optional dependency — GATE-GATED ---
    # chromadb is required ONLY if the RETR-01 gate has opened (gate-evidence.md present).
    # It is intentionally NOT installed unconditionally here. To enable vector retrieval:
    #   pip install chromadb   # first DefaultEmbeddingFunction() call caches all-MiniLM-L6-v2 (one-time network)
    # Until then, context-pull search degrades gracefully to grep — no action needed.
    ```
    Place it as comments only; do NOT add a `pip install` line that runs.
  </action>
  <verify>
    <automated>bash /Users/oanhduong/Work/mine/build-anything/scripts/retrieval-e2e-test.sh; rc=$?; echo "e2e-exit=$rc"; grep -q 'chromadb' /Users/oanhduong/Work/mine/build-anything/install.sh && [ "$rc" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: scripts/retrieval-e2e-test.sh (executable: test -x), scripts/retrieval/fixtures/corpus-seed.md, scripts/retrieval/fixtures/known-miss-query.txt
    - grep -q 'set -uo pipefail' scripts/retrieval-e2e-test.sh AND ! grep -qE '^set -e' scripts/retrieval-e2e-test.sh
    - grep -q 'mktemp -d' scripts/retrieval-e2e-test.sh (isolation)
    - grep -q 'BACKEND=grep' scripts/retrieval-e2e-test.sh (degraded-mode assertion, CHECK C)
    - grep -q 'BACKEND=vector' scripts/retrieval-e2e-test.sh (recall assertion, CHECK B)
    - grep -q 'auth-rotation' scripts/retrieval-e2e-test.sh (asserts the known-miss seed id is returned)
    - grep -q 'Results:' scripts/retrieval-e2e-test.sh (summary line)
    - corpus-seed.md does NOT contain the literal word from the query: ! grep -qi 'authentication' scripts/retrieval/fixtures/corpus-seed.md
    - known-miss-query.txt contains the query word: grep -qi 'authentication' scripts/retrieval/fixtures/known-miss-query.txt
    - install.sh contains the gate-gated chromadb comment AND has NO active pip install: grep -q 'chromadb' install.sh AND ! grep -qE '^[^#]*pip install chromadb' install.sh
    - retrieval-e2e-test.sh exits 0 (PASS path; vector checks SKIP cleanly when chromadb absent, grep + API checks still pass)
  </acceptance_criteria>
  <done>retrieval-e2e-test.sh runs the regression (grep miss vs vector hit), the degraded-to-grep proof, the API-unchanged structural check, and a latency log; it skips vector checks cleanly when chromadb is absent and exits 0; install.sh carries a gate-gated chromadb note with no unconditional install.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — full Phase 4 done command + live degraded-mode</name>
  <files>(verification only — no files modified)</files>
  <action>Human-verify checkpoint. Run the verification steps in how-to-verify below, then record the outcome (gate OPEN with e2e green, or gate CLOSED / Phase 4 deferred) in the plan SUMMARY. No code is written in this task — it gates phase completion on a human sign-off.</action>
  <what-built>
    The complete Phase 4 conditional retrieval layer: RETR-01 gate (check-retrieval-gate.sh +
    gate-evidence.md), RETR-02 vector/hybrid backend (build_index.py, search.py,
    build-retrieval-index.sh), context-pull search wired to the hybrid backend with unchanged API,
    /retro approve index-rebuild trigger, and the retrieval-e2e-test.sh done command with regression
    fixtures.
  </what-built>
  <how-to-verify>
    1. Run the gate: `./scripts/check-retrieval-gate.sh; echo "gate-exit=$?"`.
       - If it exits 0: confirm `.planning/phases/04-heavy-retrieval-conditional/gate-evidence.md`
         exists and records gate-path + measured numbers. Proceed to step 2.
       - If it exits 1 (gate CLOSED — expected near-term since real corpus is 6 entries and grep over
         a local 100-file corpus is typically <100ms): confirm NO gate-evidence.md was written. This is
         CORRECT — Phase 4 stays deferred. Record "gate closed, Phase 4 deferred" and STOP here; the
         vector backend exists but is dormant until the corpus/latency crosses threshold.
    2. (Only if gate open AND you want to exercise the live vector path) Install chromadb once:
       `pip install chromadb` (first run caches the model — needs network once).
    3. Run the full done command: `./scripts/check-retrieval-gate.sh && ./scripts/retrieval-e2e-test.sh`.
       Confirm the e2e prints `Results: N passed, 0 failed`.
    4. Live degraded-mode spot check: `rm -rf ~/.claude/.retrieval` (or leave it absent), then invoke
       `context-pull search openpyxl` from a Claude session (or run
       `python3 scripts/retrieval/search.py --query openpyxl --index-path ~/.claude/.retrieval`).
       Confirm it STILL returns output and stderr shows `BACKEND=grep` — search never hard-fails.
    Expected outcome: either (a) gate closed → Phase 4 correctly deferred, OR (b) gate open + e2e green
    + degraded mode proven. Both are valid completions.
  </how-to-verify>
  <resume-signal>Type "approved" (state whether gate opened or stayed closed), or describe issues.</resume-signal>
  <verify>Human runs the done command `./scripts/check-retrieval-gate.sh && ./scripts/retrieval-e2e-test.sh` and the live degraded-mode spot check, then confirms the outcome.</verify>
  <done>Human has signed off: either gate OPEN with `Results: N passed, 0 failed` from the e2e and BACKEND=grep proven on a removed index, OR gate CLOSED with no gate-evidence.md (Phase 4 deferred). Outcome recorded in 04-03-SUMMARY.md.</done>
</task>

</tasks>

<verification>
- retrieval-e2e-test.sh proves vector recall on a grep-missed query, degraded-to-grep on a removed index, and unchanged API — with clean SKIP when chromadb is absent
- Regression fixtures exist and encode a real grep-miss / vector-hit pair (query term absent from seed body)
- install.sh notes chromadb as gate-gated with no unconditional pip install
- Human checkpoint confirms the done command end-to-end and live degraded mode (or records the gate-closed deferral)
</verification>

<success_criteria>
- Done command `./scripts/check-retrieval-gate.sh && ./scripts/retrieval-e2e-test.sh` is runnable and green when the gate is open and chromadb installed
- Success criterion 2 (vector returns what grep missed) proven by fixture-backed CHECK B
- Success criterion 3 (100-entry latency) logged by CHECK E
- Degraded-mode contract (never hard-fails) proven by CHECK C and the live human spot check
- Phase 4 may legitimately conclude as "gate closed, deferred" — the backend is built and dormant
</success_criteria>

<output>
After completion, create `.planning/phases/04-heavy-retrieval-conditional/04-03-SUMMARY.md`.
Record the final gate state (OPEN with e2e green, or CLOSED/deferred) and the human sign-off.
</output>
