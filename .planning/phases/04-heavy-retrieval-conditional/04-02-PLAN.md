---
phase: 04-heavy-retrieval-conditional
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - scripts/retrieval/build_index.py
  - scripts/retrieval/search.py
  - scripts/build-retrieval-index.sh
  - skills/context-pull/SKILL.md
  - skills/retro/SKILL.md
autonomous: true
requirements: [RETR-02]
user_setup:
  - service: chromadb
    why: "Local embedded vector store for the failure-lib + docs/ index (no server, no API key)"
    env_vars: []
    dashboard_config:
      - task: "Run `pip install chromadb` once (only after the RETR-01 gate opens). First DefaultEmbeddingFunction() call downloads/caches the all-MiniLM-L6-v2 ONNX model (one-time network), then runs fully offline."
        location: "Local machine shell"

must_haves:
  truths:
    - "build-retrieval-index.sh rebuilds a chromadb index over failure-lib/ + docs/ from scratch (idempotent full rebuild)"
    - "search.py returns vector results when best similarity > 0.7, else falls back to grep over the original target set"
    - "context-pull search uses the vector/hybrid backend but its external API (argument-hint, output format) is unchanged"
    - "context-pull search still returns results when the index is missing/stale — degraded to grep, never hard-fails"
    - "/retro approve calls build-retrieval-index.sh after committing a new lesson so the index stays in sync"
  artifacts:
    - path: "scripts/retrieval/build_index.py"
      provides: "chromadb PersistentClient index build over failure-lib + docs, DefaultEmbeddingFunction"
      contains: "PersistentClient"
    - path: "scripts/retrieval/search.py"
      provides: "Hybrid query: vector if similarity>0.7 else grep fallback; never hard-fails"
      contains: "query_texts"
    - path: "scripts/build-retrieval-index.sh"
      provides: "Standalone index builder (auto-distill.sh pattern), shells out to build_index.py"
      contains: "build_index.py"
    - path: "skills/context-pull/SKILL.md"
      provides: "search subcommand upgraded to hybrid backend, API unchanged"
      contains: "search.py"
    - path: "skills/retro/SKILL.md"
      provides: "approve subcommand triggers index rebuild after commit"
      contains: "build-retrieval-index.sh"
  key_links:
    - from: "skills/context-pull/SKILL.md"
      to: "scripts/retrieval/search.py"
      via: "search subcommand shells out to python search.py"
      pattern: "search\\.py"
    - from: "skills/retro/SKILL.md"
      to: "scripts/build-retrieval-index.sh"
      via: "approve calls index rebuild after git commit"
      pattern: "build-retrieval-index\\.sh"
    - from: "scripts/build-retrieval-index.sh"
      to: "scripts/retrieval/build_index.py"
      via: "shell wraps python (ENFC-04: python only in scripts/, never hooks/)"
      pattern: "build_index\\.py"
---

<objective>
Replace the grep body of the `context-pull search` subcommand with a chromadb
vector/hybrid backend over `failure-lib/` + `docs/`, behind an unchanged external API,
with grep as a safety-net fallback. Add the standalone index builder and wire
`/retro approve` to rebuild the index after committing new lessons.

GATE: This plan MUST NOT begin until Plan 04-01 reports the RETR-01 gate OPEN with
`gate-evidence.md` present. If the gate is CLOSED, defer this plan indefinitely.

Purpose: RETR-02 — semantic recall for queries grep provably misses, while preserving
exact-match precision (hybrid) and the unchanged `context-pull search` interface so no
downstream caller needs updating.

Output: `scripts/retrieval/build_index.py`, `scripts/retrieval/search.py`,
`scripts/build-retrieval-index.sh`, and edits to `skills/context-pull/SKILL.md` +
`skills/retro/SKILL.md`.
</objective>

<execution_context>
@/Users/oanhduong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oanhduong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-heavy-retrieval-conditional/04-CONTEXT.md
@.planning/phases/04-heavy-retrieval-conditional/04-RESEARCH.md
@.planning/phases/04-heavy-retrieval-conditional/gate-evidence.md

<interfaces>
<!-- GATE PRECHECK: this plan is invalid to execute unless gate-evidence.md exists. -->
<!-- First action of any executor: test -f .planning/phases/04-heavy-retrieval-conditional/gate-evidence.md — if absent, STOP and report gate closed. -->

From hooks/common.sh — emit() available:
```bash
emit() { echo "${1}" >&2; }
```

From skills/context-pull/SKILL.md — the CURRENT grep search body being replaced (keep as fallback target set):
```
grep -rn "$ARGUMENTS" docs/ .progress/ 2>/dev/null | head -40
```

From skills/retro/SKILL.md — approve step 3 commit (insert rebuild AFTER this line):
```bash
git -C "$HOME/.claude" add "failure-lib/<id>.md" \
  && git -C "$HOME/.claude" commit -m "retro: approve lesson <id> $(date -u +%Y-%m-%dT%H:%M:%SZ)"
```

From scripts/auto-distill.sh — the standalone-script pattern to mirror (shell wraps logic, set -uo pipefail, header "# tag: architecture", sources common.sh).

A failure-lib entry (the corpus document shape to index):
```
---
id: openpyxl-engine
tags: [python, pandas, testing]
when: on-error
error-match: xlrd
---
## What happened ...
## How to avoid ...
```

chromadb verified API shapes (RESEARCH.md — confirm method names against installed version at impl time):
```python
import chromadb
from chromadb.utils import embedding_functions
client = chromadb.PersistentClient(path="<HOME>/.claude/.retrieval")
ef = embedding_functions.DefaultEmbeddingFunction()   # all-MiniLM-L6-v2 via onnxruntime
col = client.get_or_create_collection(name="harness", embedding_function=ef,
                                      metadata={"hnsw:space": "cosine"})
col.add(documents=[text], ids=["failure-lib/openpyxl-engine"], metadatas=[{"source":"failure-lib"}])
res = col.query(query_texts=[q], n_results=5)   # res["distances"][0], res["documents"][0], res["metadatas"][0]
# cosine: similarity = 1 - distance
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: build_index.py + build-retrieval-index.sh — chromadb index over failure-lib + docs</name>
  <files>scripts/retrieval/build_index.py, scripts/build-retrieval-index.sh</files>
  <read_first>
    - scripts/auto-distill.sh (standalone-script-shelled-from-skill pattern; set -uo pipefail; "# tag: architecture" header; sources common.sh)
    - install.sh (how scripts/ are installed: full overwrite + chmod +x; ~/.claude path conventions)
    - failure-lib/openpyxl-engine.md (the document shape: YAML frontmatter + body; the id is the filename stem)
    - .planning/phases/04-heavy-retrieval-conditional/04-RESEARCH.md (Pattern 3 idempotent full rebuild; Pitfall 2 ONE embedding function for build+query; Pitfall 4 cold-start model download; Pattern 2 ENFC-04 boundary — python only in scripts/)
    - .planning/phases/04-heavy-retrieval-conditional/04-CONTEXT.md (index at ~/.claude/.retrieval/; DefaultEmbeddingFunction per Claude Discretion; source metadata distinguishes failure-lib vs docs)
  </read_first>
  <action>
    Create `scripts/retrieval/build_index.py`:
    - CLI: `python3 build_index.py [--lib-dir DIR] [--docs-dir DIR] [--index-path DIR]`
      Defaults: --lib-dir $HOME/.claude/failure-lib, --docs-dir $HOME/.claude/docs
      (fall back to repo-local failure-lib/ and docs/ if the HOME dirs do not exist),
      --index-path $HOME/.claude/.retrieval.
    - `import chromadb` and `from chromadb.utils import embedding_functions`.
    - `client = chromadb.PersistentClient(path=index_path)` (mkdir parents first; creates dir on first run).
    - `ef = embedding_functions.DefaultEmbeddingFunction()` — all-MiniLM-L6-v2 via onnxruntime.
      Use this EXACT function in BOTH build_index.py and search.py (Pitfall 2 — never mix embedders).
      Do NOT use SentenceTransformerEmbeddingFunction (chromadb issue #2748 incompatibility).
    - Idempotent full rebuild (Pattern 3): `try: client.delete_collection("harness") except Exception: pass`
      then `col = client.get_or_create_collection(name="harness", embedding_function=ef, metadata={"hnsw:space":"cosine"})`.
    - Walk --lib-dir for *.md (EXCLUDE the pending/ subdir and .gitkeep) and --docs-dir for *.md
      (recursive; if docs is empty or only .gitkeep, just skip — must NOT error on empty docs/).
      For each file: read full text; id = "<source>/<filename-stem>" (e.g. failure-lib/openpyxl-engine);
      metadata = {"source": "<failure-lib|docs>", "path": "<relpath>", "lesson_id": "<stem>"}.
    - Batch `col.add(documents=[...], ids=[...], metadatas=[...])`. If the corpus is EMPTY (0 docs),
      print a warning to stderr and exit 0 (empty index is valid — search will fall back to grep).
    - Print `INDEXED=<count>` to stdout and exit 0. On chromadb ImportError, print
      `chromadb not installed — run: pip install chromadb` to stderr and exit 3 (distinct code so
      the shell wrapper reports a clear install hint).
    - Keep deps to chromadb + stdlib only (argparse, pathlib, os) — no sentence-transformers, no torch.

    Create `scripts/build-retrieval-index.sh` (chmod +x), mirroring auto-distill.sh:
    - Header `# tag: architecture` (ENFC-02 — permanent infra). `set -uo pipefail`. `source` common.sh.
    - `HARNESS_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"`.
    - This is a SCRIPT (not a hook) so it MAY call python3:
      `python3 "$HARNESS_DIR/scripts/retrieval/build_index.py" "$@"`; capture exit code into RC.
    - If RC == 3 (chromadb missing): `emit "retrieval index skipped — chromadb not installed (pip install chromadb)"` and exit 0
      (do NOT fail the caller — /retro approve must still succeed even if the index can't be built).
    - On RC == 0: `emit "retrieval index rebuilt"` and exit 0. On any other RC: emit the error and exit 0
      (index build is best-effort; never break the calling skill).
  </action>
  <verify>
    <automated>bash /Users/oanhduong/Work/mine/build-anything/scripts/build-retrieval-index.sh --lib-dir /Users/oanhduong/Work/mine/build-anything/failure-lib --docs-dir /Users/oanhduong/Work/mine/build-anything/docs --index-path /tmp/test-retrieval-idx; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: scripts/retrieval/build_index.py and scripts/build-retrieval-index.sh (executable: test -x scripts/build-retrieval-index.sh)
    - grep -q 'PersistentClient' scripts/retrieval/build_index.py
    - grep -q 'DefaultEmbeddingFunction' scripts/retrieval/build_index.py AND ! grep -q 'SentenceTransformerEmbeddingFunction' scripts/retrieval/build_index.py (Pitfall 2)
    - grep -q 'delete_collection' scripts/retrieval/build_index.py (idempotent rebuild)
    - grep -q 'hnsw:space' scripts/retrieval/build_index.py (cosine space configured)
    - grep -q '# tag: architecture' scripts/build-retrieval-index.sh AND grep -q 'set -uo pipefail' scripts/build-retrieval-index.sh
    - grep -q 'build_index.py' scripts/build-retrieval-index.sh (shell wraps python)
    - Verify command exits 0 whether or not chromadb is installed (RC 3 from python is swallowed to 0 by the wrapper with a skip message)
  </acceptance_criteria>
  <done>build_index.py builds a chromadb cosine index over failure-lib + docs using DefaultEmbeddingFunction with idempotent full rebuild and clean handling of empty docs/ and missing chromadb; build-retrieval-index.sh wraps it (auto-distill.sh pattern), never hard-fails the caller when chromadb is absent.</done>
</task>

<task type="auto">
  <name>Task 2: search.py hybrid query + wire context-pull search and retro approve</name>
  <files>scripts/retrieval/search.py, skills/context-pull/SKILL.md, skills/retro/SKILL.md</files>
  <read_first>
    - scripts/retrieval/build_index.py (MUST reuse the identical DefaultEmbeddingFunction + collection name "harness" + index path — Pitfall 2)
    - skills/context-pull/SKILL.md (the grep fallback target set: docs/ .progress/; the unchanged external API to preserve)
    - skills/retro/SKILL.md (the approve step 3 commit line to insert the rebuild after)
    - .planning/phases/04-heavy-retrieval-conditional/04-RESEARCH.md (Pattern 4 hybrid scoring similarity>0.7; Open Q3 cosine similarity = 1 - distance; Open Q4 corpus mismatch — vector indexes failure-lib+docs, grep fallback keeps its ORIGINAL docs/ .progress/ target set; Anti-pattern: never hard-fail, never auto-rebuild mid-session)
    - .planning/phases/04-heavy-retrieval-conditional/04-CONTEXT.md (stale-index detection emits warning, does NOT rebuild)
  </read_first>
  <action>
    Create `scripts/retrieval/search.py`:
    - CLI: `python3 search.py --query "<text>" [--index-path DIR] [--lib-dir DIR] [--n 5]`
      Defaults match build_index.py ($HOME/.claude/.retrieval, $HOME/.claude/failure-lib).
    - Hybrid logic (Pattern 4):
      1. VECTOR path (wrap entire block in try/except): import chromadb; open
         `PersistentClient(path=index_path)`; `col = client.get_collection(name="harness",
         embedding_function=DefaultEmbeddingFunction())` (SAME embedder as build — Pitfall 2).
         Run `res = col.query(query_texts=[query], n_results=n)`. Convert each cosine distance to
         similarity = 1 - distance. Keep results with similarity > 0.7.
      2. If >= 1 result has similarity > 0.7: print each as
         `<similarity:.3f>\t<source>/<lesson_id>\t<first ~120 chars of document>`, print
         `BACKEND=vector` to stderr, exit 0.
      3. ELSE (no result over 0.7, OR chromadb ImportError, OR index missing/unopenable, OR ANY
         exception): FALL BACK to grep. Run the ORIGINAL command via subprocess:
         `grep -rn "<query>" docs/ .progress/ 2>/dev/null | head -40` (grep-fallback dirs resolved
         relative to CWD — preserve the Phase 2 target set; do NOT add failure-lib to the grep
         fallback, it is already surfaced by load-lessons.sh — RESEARCH Open Q4). Print grep stdout,
         print `BACKEND=grep` to stderr, exit 0.
    - The script MUST NEVER exit non-zero on a missing/stale index or missing chromadb — degraded grep
      is the contract (CONTEXT specifics; RESEARCH anti-pattern).
    - Stale detection (warning only, no rebuild): if the index path exists and its newest mtime is
      OLDER than the newest *.md mtime under --lib-dir, print
      `STALE-INDEX: run build-retrieval-index.sh to refresh` to stderr — but STILL serve the query
      (do not rebuild mid-search — CONTEXT forbids the latency spike).

    EDIT `skills/context-pull/SKILL.md` `## search <query>` subcommand:
    - Keep the frontmatter `argument-hint` and the file `description` EXACTLY as-is (RETR-02: external API unchanged).
    - Replace the `grep -rn "$ARGUMENTS" docs/ .progress/ 2>/dev/null | head -40` command block body with:
      ```
      python3 ~/.claude/scripts/retrieval/search.py --query "$ARGUMENTS"
      ```
      (Strip the leading "search " word before passing the remaining text as the query, as today.
      If running from the source repo, use `python3 scripts/retrieval/search.py --query "..."`.)
    - Add a one-line note under the search heading: "Hybrid backend — vector results when confident,
      otherwise grep fallback over docs/ and .progress/. Never hard-fails: a missing or stale index
      degrades to grep." Leave `get-file` and `expand-summary` subcommands UNCHANGED.

    EDIT `skills/retro/SKILL.md` `## /retro approve` step 3: AFTER the existing
    `git -C "$HOME/.claude" ... commit ...` line, add a new line:
    ```
    bash "$HOME/.claude/scripts/build-retrieval-index.sh"   # rebuild index so the new lesson is searchable (RETR-02)
    ```
    Do not change any other retro subcommand or any other context-pull subcommand.
  </action>
  <verify>
    <automated>python3 /Users/oanhduong/Work/mine/build-anything/scripts/retrieval/search.py --query "openpyxl" --index-path /tmp/nonexistent-idx 2>&1 | grep -q 'BACKEND=grep'; rc=$?; grep -q 'search.py' /Users/oanhduong/Work/mine/build-anything/skills/context-pull/SKILL.md && grep -q 'build-retrieval-index.sh' /Users/oanhduong/Work/mine/build-anything/skills/retro/SKILL.md && [ $rc -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - File exists: scripts/retrieval/search.py
    - grep -q 'query_texts' scripts/retrieval/search.py (vector query) AND grep -q 'DefaultEmbeddingFunction' scripts/retrieval/search.py (same embedder as build — Pitfall 2)
    - grep -q 'BACKEND=grep' scripts/retrieval/search.py AND grep -q 'BACKEND=vector' scripts/retrieval/search.py
    - grep -q "0.7" scripts/retrieval/search.py (similarity threshold)
    - Running search.py against a NON-EXISTENT index prints BACKEND=grep and exits 0 (verify command above)
    - grep -q 'search.py' skills/context-pull/SKILL.md (search subcommand wired to hybrid backend)
    - argument-hint line in skills/context-pull/SKILL.md still reads `argument-hint: "search <query> | get-file <path> | expand-summary <section>"` (API unchanged): grep -q 'search <query> | get-file' skills/context-pull/SKILL.md
    - grep -q 'build-retrieval-index.sh' skills/retro/SKILL.md (approve triggers rebuild)
  </acceptance_criteria>
  <done>search.py serves a hybrid query (vector when similarity>0.7, grep fallback otherwise) and never hard-fails on a missing/stale index; context-pull search is wired to it with its external API unchanged; /retro approve rebuilds the index after commit.</done>
</task>

</tasks>

<verification>
- build_index.py + build-retrieval-index.sh build a chromadb cosine index over failure-lib + docs, idempotent, empty-corpus-safe, chromadb-missing-safe
- search.py returns vector results above the 0.7 similarity threshold, else degrades to grep over docs/ .progress/ — never hard-fails
- context-pull search external API (argument-hint, output format) is byte-unchanged; only the backend swapped
- /retro approve rebuilds the index after committing a lesson
- ENFC-04 preserved: no python token leaks into hooks/ (all python lives in scripts/retrieval/ and is invoked only from scripts/*.sh and skills)
</verification>

<success_criteria>
- RETR-02 vector/hybrid index over failure-lib + docs/ replaces grep in context-pull search
- Hybrid scoring honored: vector when best similarity > 0.7, grep fallback otherwise
- Degraded mode proven: missing index → grep fallback, search still returns, exit 0
- Index stays in sync: /retro approve triggers build-retrieval-index.sh
</success_criteria>

<output>
After completion, create `.planning/phases/04-heavy-retrieval-conditional/04-02-SUMMARY.md`.
Record whether chromadb was installed during execution and whether the vector path was
exercised live or only the grep-fallback path was verified (Plan 03 e2e will exercise both).
</output>
